# 远程控制

`--remote` 参数启用远程控制功能，可通过手机扫码在网页端控制终端中的 AI 对话：

| 命令 | 说明 |
|------|------|
| `j ai --remote` | 启用远程控制（默认端口 9390） |
| `j ai --remote --port 8080` | 自定义端口 |
| `j ai -c --remote` | 延续会话 + 远程控制 |

启动后会显示二维码，手机扫描后即可在浏览器中操作。

> `--remote` 模式会强制进入 TUI 界面，忽略消息内容参数