# 归档 & 恢复

## 归档对话（/archive）

- 输入 `/archive` 后确认，当前对话会被保存到归档
- 默认归档名称格式：`archive-YYYY-MM-DD`
- 如果同名归档已存在，自动添加后缀（如 `archive-2026-02-25(1)`）
- 归档后当前会话自动清空

## 恢复会话（/resume）

- 输入 `/resume` 进入会话列表
- 使用 `↑` / `↓` 或 `j` / `k` 选择历史会话
- 按 `Enter` 恢复选中的会话