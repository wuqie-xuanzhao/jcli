# JCLI Roadmap

本文档记录 jcli 项目的未来发展方向与规划。

---

## 1. 集成 j-gui 仓库

**目标**：将独立的 [j-gui](https://github.com/LingoJack/j-gui) 前端仓库（`web/`）集成到 jcli 主仓库（[j](https://github.com/LingoJack/j)）中。

**背景**：
- j-gui 当前作为独立仓库维护，与 jcli 主仓库分离
- 两个仓库独立管理增加了版本同步和发布复杂度
- 合并后可统一构建流程、减少跨仓库协调成本

**具体任务**：
- [ ] 将 j-gui 代码完整迁入 jcli 仓库的 `web/` 目录
- [ ] 统一 j-gui 与 jcli 的版本号管理
- [ ] 在 `Makefile` 中集成前端构建命令，`make build` 时自动构建前端产物
- [ ] 统一 CI/CD 流程，前端与后端同步发布
- [ ] 退役 j-gui 独立仓库（归档）

---

## 2. 终端 Markdown 编辑器快捷格式支持

**目标**：为 TUI Markdown 编辑器增加类似 Obsidian / Notion 的斜杠快捷格式命令。

**背景**：
- 当前编辑器支持基础 Markdown 语法，但缺少快捷操作入口
- 用户需要手动输入 `**bold**`、`![image]()` 等语法，效率较低
- 相关代码：`src/tui/editor_core/`、`src/markdown/`

**具体任务**：
- [ ] 实现斜杠命令菜单（`/` 触发）
  - `/h1` → `# `
  - `/h2` → `## `
  - `/bold` → `****`（选中文字包裹）
  - `/italic` → `__`（选中文字包裹）
  - `/code` → `` ` `` 或代码块
  - `/link` → `[]()`
  - `/image` → `![]()`
  - `/list` → `- `
  - `/todo` → `- [ ] `
- [ ] 支持选中文字后应用格式包裹
- [ ] 命令菜单 UI：弹出浮层展示可选项，键盘导航选择
- [ ] 命令可配置：允许用户自定义快捷命令映射

---

## 3. GUI 设计全面简化

**目标**：精简 j-gui 界面设计，降低维护成本，提升开发效率。

**背景**：
- 当前 GUI 设计元素较多，视觉复杂度较高
- 部分装饰性组件维护成本大于用户价值
- 简化设计有助于更快推进后续功能开发（代码编辑、PPT 编辑等）

**具体任务**：
- [ ] 梳理现有 GUI 组件，移除冗余装饰性元素
- [ ] 简化布局结构，减少嵌套层级
- [ ] 统一配色与字体规范，输出 Design Tokens
- [ ] 减少动画效果，保留必要的交互反馈
- [ ] 移除低使用率的功能入口

---

## 4. GUI 代码编辑与 HTML PPT 编辑功能

**目标**：为 j-gui 添加代码编辑器和 HTML PPT 编辑功能，使其从辅助交互工具升级为可用的编辑环境。

**背景**：
- 当前 GUI 缺少核心编辑能力，用户仍需切换到外部工具
- HTML PPT（`presentation/ppt/`）是 jcli 的特色产出，需要可视化编辑支持
- 相关 Skill：`html-ppt`

### 4.1 代码编辑器

- [ ] Monaco Editor / CodeMirror 集成
- [ ] 支持 jcli 日常开发语言：Rust、Go、TypeScript、Markdown
- [ ] 语法高亮 + 基础错误提示（LSP 可选）
- [ ] 与 TUI 编辑器的数据同步：编辑同一文件时状态一致

### 4.2 HTML PPT 编辑器

- [ ] 基于 `html-ppt` Skill 的可视化编辑界面
- [ ] 功能清单：
  - [ ] 幻灯片列表视图（左侧边栏）
  - [ ] 单页内容编辑（Markdown / HTML 源码双模式）
  - [ ] 实时预览（右侧面板）
  - [ ] 主题切换（继承现有 CSS 主题体系）
  - [ ] 幻灯片排序、新增、删除
  - [ ] 导出：生成完整 HTML 文件
- [ ] 与 Skill 系统联动：GUI 编辑调用 `html-ppt` Skill 生成产物

---

## 优先级排序

| 序号 | 任务 | 优先级 | 估算工期 |
|---|---|---|---|
| 1 | 集成 j-gui 仓库 | P1 | 1 周 |
| 2 | 终端 Markdown 快捷格式 | P2 | 2 周 |
| 3 | GUI 设计简化 | P2 | 1 周 |
| 4 | GUI 代码编辑器 | P3 | 3 周 |
| 5 | HTML PPT 编辑器 | P3 | 4 周 |

---

## 状态追踪

本文档随项目进展更新。已完成的任务将标记为 `[x]`。
