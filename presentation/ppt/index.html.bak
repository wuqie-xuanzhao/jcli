<!--
  毕设答辩 PPT · 基于多 Agent 协同的 LLM 驱动复杂业务软件自动化生成的研究与实现

  生成方式：html-ppt skill
  相关文件：
    - 毕业论文：bishe/202230500125_温俊杰_本科毕业设计.docx
    - 演说 idea：presentation/ppt/idea.md
    - 截图素材：presentation/ppt/pics/
    - 自定义样式：presentation/ppt/style.css

  幻灯片结构（28 张内容页 + Cover/Thanks）：
    §01 项目与背景（9 页）— 项目介绍 / Agenda / ChatBot vs Agent / ReAct 演示 5 帧
    §02 上下文治理与状态感知（5 页）— 问题 / 窗口膨胀 / 压缩系统 / 优先级漏斗 / 动态 System Prompt
    §03 面向场景强化的工具（12 页）— 总览 / Grep&Ask / Plan+审批 / Plan执行 / Edit精确替换+批量二阶段握手 / 执行三态 2 帧 / TodoWrite / Hook
    §04 多 Agent 协作试验（6 页）— 总览 / 消息设计 / 轮流报数 / 续写接龙 / 协作开发 / SubAgent
    §05 模板代码生成引擎（4 页）— 动机 / jen 架构 / 实测对比 / webapp-gen
    §06 总结与展望（2 页）— 仓库 / 四大贡献
-->
<!DOCTYPE html>
<html lang="zh-CN" data-themes="academic-paper,minimal-white,swiss-grid,corporate-clean">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>本科毕设答辩 · 基于 LLM Agent 的编程辅助工具设计与实现</title>
<link rel="stylesheet" href="../../.jcli/skills/html-ppt-skill/assets/fonts.css">
<link rel="stylesheet" href="../../.jcli/skills/html-ppt-skill/assets/base.css">
<link rel="stylesheet" id="theme-link" href="../../.jcli/skills/html-ppt-skill/assets/themes/academic-paper.css">
<link rel="stylesheet" href="../../.jcli/skills/html-ppt-skill/assets/animations/animations.css">
<link rel="stylesheet" href="style.css">
</head>
<body class="tpl-presenter-mode-reveal">
<div class="deck">

  <!-- ============ 1. COVER ============ -->
  <section class="slide cover" data-title="Cover">
    <p class="kicker">本科毕业设计 · 2026</p>
    <h1 class="h1 anim-fade-up" data-anim="fade-up">基于多 Agent 协同的<br><span class="accent">LLM 驱动复杂业务软件</span><br>自动化生成的研究与实现</h1>
    <p class="lede mt-m">从 ChatBot 到 Agent：让 AI 真正参与软件工程全流程</p>
    <div class="speaker">
      <div><b>答辩人：温俊杰 &nbsp; 202230500125</b></div>
      <div><b>指导老师：黄小兵</b></div>
    </div>
    <div class="deck-footer">
      <span class="mono">github.com/LingoJack/jcli</span>
      <span class="slide-number" data-current="1" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>各位老师好，我是温俊杰。我的毕设题目是《基于多 Agent 协同的 LLM 驱动复杂业务软件自动化生成的研究与实现》。</p>
    </aside>
  </section>

  <!-- ============ 1.5 PROJECT INTRO ============ -->
  <section class="slide" data-title="项目">
    <div class="title-bar">
      <h2>01 · 项目与背景 — jcli · AI 终端工作站
    </div>
    <div class="split s55">
      <div class="col-text" style="overflow:hidden">
        <h4 style="margin-top:0;margin-bottom:2px"><span class="accent">jcli</span> · 主项目 · Rust · 5.2 万行</h4>
        <p style="margin:0 0 2px;font-size:13px;line-height:1.6;color:var(--text-2)">多模型终端 Agent。包含<strong>上下文治理</strong>（分级压缩 + 优先级窗口 + 动态 System Prompt）、<strong>面向场景强化的工具</strong>（Plan / Edit / 执行三态 / system-reminder）、<strong>多 Agent 协作</strong>（Teammate + SubAgent），以及 Skill / Hook 工程化扩展机制。</p>
        <ul class="repo-list">
          <li>
            <div class="rl-row"><span class="rl-label">jcli · 主项目</span><span class="rl-url">github.com/LingoJack/jcli</span></div>
          </li>
        </ul>

        <h4 style="margin-bottom:2px"><span class="accent">jen</span> · 模板代码引擎 · Go · model_infrax</h4>
        <p style="margin:0 0 2px;font-size:13px;line-height:1.6;color:var(--text-2)">自研 SQL parser + Go template，将 SQL schema 确定性生成 PO/DTO/VO/DAO 全套方法。按主键 / 唯一索引 / 普通索引分别派生查询方法，消除 LLM 长链路推理中的字段漂移。封装为 Skill，按需加载。</p>
        <ul class="repo-list">
          <li>
            <div class="rl-row"><span class="rl-label">jen · 模板代码引擎</span><span class="rl-url">github.com/LingoJack/model_infrax</span></div>
          </li>
        </ul>

        <h4 style="margin-bottom:2px"><span class="accent">artifact_demo</span> · 端到端验证制品</h4>
        <p style="margin:0 0 2px;font-size:13px;line-height:1.6;color:var(--text-2)">用 jcli 配合 webapp-gen 和 jen 从零生成的电商 Web App（React + Go + GORM + Podman），三环境矩阵验收全部通过。</p>
        <ul class="repo-list">
          <li>
            <div class="rl-row"><span class="rl-label">artifact_demo · 验证制品</span><span class="rl-url">github.com/LingoJack/gradiation_artifact_demo</span></div>
          </li>
        </ul>

        <p class="note" style="margin-top:auto">三个仓库均已开源 · MIT License</p>
      </div>
      <div class="col-visual">
        <div class="stack-figs">
          <a class="sf sf-3" style="width:68%;top:40%" href="https://github.com/LingoJack/gradiation_artifact_demo" target="_blank" rel="noopener">
            <img src="pics/jcli-demo.png" alt="效果演示仓库">
            <div class="caption">
              <span class="name">artifact_demo · 端到端验证制品</span>
              <span class="url">github.com/LingoJack/gradiation_artifact_demo</span>
            </div>
          </a>
          <a class="sf sf-2" style="width:68%;top:18%" href="https://github.com/LingoJack/model_infrax" target="_blank" rel="noopener">
            <img src="pics/github-model-infrax.png" alt="model_infrax 仓库">
            <div class="caption">
              <span class="name">model_infrax · 模板代码引擎</span>
              <span class="url">github.com/LingoJack/model_infrax</span>
            </div>
          </a>
          <a class="sf sf-1" style="width:68%" href="https://github.com/LingoJack/jcli" target="_blank" rel="noopener">
            <img src="pics/github-jcli.png" alt="jcli 仓库">
            <div class="caption">
              <span class="name">jcli · 主项目仓库</span>
              <span class="url">github.com/LingoJack/jcli</span>
            </div>
          </a>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">jcli · model_infrax · artifact_demo</span>
      <span class="slide-number" data-current="2" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>这是本次毕设的三个产出。其中 jcli 是我自己用 Rust 语言实现的 Agent，也是主要实验载体。后面说的上下文治理、工具强化、多 Agent 协作这些设计，都是在 jcli 这个仓库里实现和实验的；jen 是我写的一个模板代码生成引擎；最后是用这两者端到端生成出来的一个电商 Web App 演示项目。</p>
    </aside>
  </section>

  <!-- ============ 2. AGENDA ============ -->
  <section class="slide" data-title="Agenda">
    <div class="title-bar">
      <h2>01 · 项目与背景 — 今天要讲的 6 个部分
    </div>
    <div class="stack mt-l">
      <!-- 这个是我敲定的大纲，不要改 -->
      <div class="agenda-row"><span class="num">01</span><span class="t">项目与背景</span><span class="d">~4min</span></div>
      <div class="agenda-row"><span class="num">02</span><span class="t">上下文治理与状态感知</span><span class="d">~6min</span></div>
      <div class="agenda-row"><span class="num">03</span><span class="t">面向场景强化的工具</span><span class="d">~8min</span></div>
      <div class="agenda-row"><span class="num">04</span><span class="t">多 Agent 协作试验</span><span class="d">~5min</span></div>
      <div class="agenda-row"><span class="num">05</span><span class="t">模板代码生成引擎</span><span class="d">~4min</span></div>
      <div class="agenda-row"><span class="num">06</span><span class="t">总结与展望</span><span class="d">~3min</span></div>
    </div>
    <aside class="notes">
      <p>下面我会按背景、上下文治理、工具强化、多 Agent 协作、模板代码生成、最后总结这六个部分展开汇报。</p>
    </aside>
  </section>

  <!-- ============ 3. BACKGROUND: ChatBot vs Agent ============ -->
  <section class="slide" data-title="ChatBot vs Agent">
    <div class="title-bar">
      <h2>01 · 项目与背景 — ChatBot 与 Agent 的上下文成本对比
    </div>
    <div class="vs" style="margin-top:4px">
      <div class="bad-side side">
        <h3 style="margin-bottom:2px">ChatBot</h3>
        <img src="pics/chatbot.png" alt="ChatBot 交互模式" style="width:100%;border-radius:4px;margin:2px 0 4px;border:1px solid var(--border)">
        <ul style="line-height:1.6;margin:0;font-size:14px">
          <li>人手动收集代码、报错、配置</li>
          <li>复制粘贴到对话窗口</li>
          <li>AI 给建议，人手动改代码</li>
          <li>人承担<strong>上下文收集</strong>和<strong>执行反馈</strong></li>
          <li>本质：人是物理/逻辑世界的桥梁</li>
        </ul>
      </div>
      <div class="mid">→</div>
      <div class="good-side side">
        <h3 style="margin-bottom:2px">Agent</h3>
        <img src="pics/agent.png" alt="Agent 交互模式" style="width:100%;border-radius:4px;margin:2px 0 4px;border:1px solid var(--border)">
        <ul style="line-height:1.6;margin:0;font-size:14px">
          <li>Agent 通过工具主动读取、执行</li>
          <li>Agent 自动管理上下文窗口</li>
          <li>Agent 自主决策、执行、纠错</li>
          <li>Agent 承担<strong>上下文治理</strong>和<strong>工具调用</strong></li>
          <li>本质：Agent 成为桥梁，人抽离</li>
        </ul>
      </div>
    </div>
    <aside class="notes">
      <p>Agent 相比于 ChatBot 可以使用工具主动收集上下文、操作本地文件。</p>
    </aside>
  </section>

  <!-- ============ 3.5 FUNCTION CALL ============ -->
  <section class="slide" data-title="Function Call">
    <div class="title-bar">
      <h2>01 · 项目与背景 — Function Calling：LLM 如何调用工具
    </div>
    <div class="split s55">
      <div class="col-text" style="gap:10px">
        <p class="lead" style="font-size:19px">LLM 是<strong>文本预测器</strong>，不能直接操作文件或终端。<span class="accent">Function Calling</span> 是"说话"到"做事"的桥梁。</p>
        <h4 style="margin-top:4px">LLM 的输入结构</h4>
        <p>每次请求，LLM 收到一份 <strong>system prompt</strong> + 一组 <strong>messages</strong>：</p>
        <ul style="line-height:1.7;margin:0">
          <li><strong>User</strong> · 用户输入 + 系统注入的提醒</li>
          <li><strong>Assistant</strong> · LLM 回复，含文本和 tool_calls</li>
          <li><strong>Tool Result</strong> · 工具执行结果回灌</li>
        </ul>
        <h4 style="margin-top:4px">Function Calling 流程</h4>
        <ul style="line-height:1.7;margin:0">
          <li><strong>工具注册</strong> · 名称、描述、参数 Schema 写入 System Prompt</li>
          <li><strong>LLM 决策</strong> · 输出 <strong>tool_calls JSON</strong>（工具名 + 参数）</li>
          <li><strong>运行时执行</strong> · 解析 JSON，执行工具，结果回灌 messages</li>
          <li><strong>循环迭代</strong> · LLM 看到结果后继续推理，可能再次调工具</li>
        </ul>
        <p class="note" style="padding-top:6px;margin-top:4px">关键：LLM 只产出"意图"，运行时代替它执行。</p>
      </div>
      <div class="col-visual">
        <div class="term" style="margin-top:0">
          <div class="term-bar"><span>LLM response · function call</span><span>JSON</span></div>
          <div class="term-body" style="font-size:11px;line-height:1.5">
            <div class="bubble agent is-new">
              <div class="who"><span>LLM 输出</span><span class="tag">tool_calls</span></div>
<div class="body" style="font-family:var(--font-mono);font-size:10px">{
  <span class="k">"name"</span>: <span class="v">"Bash"</span>,
  <span class="k">"arguments"</span>: {
    <span class="k">"command"</span>: <span class="v">"ls -F src/command/chat"</span>,
    <span class="k">"description"</span>: <span class="v">"List chat module entries"</span>
  }
}</div>
            </div>
            <div class="bubble tool-result is-new" style="margin-top:6px">
              <div class="who"><span>运行时执行</span><span class="tag">tool_result</span></div>
<div class="body">agent.rs  app.rs  compact.rs  hook.rs<br>handler/  markdown/  tools/  ui/</div>
            </div>
          </div>
        </div>
        <div class="fig" data-fig="图 · Function Calling 流程" style="margin-top:10px">
          <svg viewBox="0 0 380 100" style="width:100%;height:auto;font-family:var(--font-mono);font-size:10px">
            <defs>
              <marker id="ah-fc" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
                <path d="M0,0 L10,5 L0,10 z" fill="#1e3a8a"/>
              </marker>
            </defs>
            <rect x="10" y="15" width="70" height="50" fill="#f5f8ff" stroke="#1e3a8a" stroke-width="1.2" rx="3"/>
            <text x="45" y="36" text-anchor="middle" font-weight="600" fill="#0d1b2a" font-size="10">工具注册</text>
            <text x="45" y="52" text-anchor="middle" fill="#6b7a8f" font-size="8">Schema→LLM</text>
            <line x1="80" y1="40" x2="100" y2="40" stroke="#1e3a8a" stroke-width="1.2" marker-end="url(#ah-fc)"/>
            <rect x="100" y="15" width="70" height="50" fill="#f5f8ff" stroke="#1e3a8a" stroke-width="1.2" rx="3"/>
            <text x="135" y="36" text-anchor="middle" font-weight="600" fill="#0d1b2a" font-size="10">LLM 推理</text>
            <text x="135" y="52" text-anchor="middle" fill="#6b7a8f" font-size="9">输出 JSON</text>
            <line x1="170" y1="40" x2="190" y2="40" stroke="#1e3a8a" stroke-width="1.2" marker-end="url(#ah-fc)"/>
            <rect x="190" y="15" width="80" height="50" fill="#fbfdfb" stroke="#1e5e3a" stroke-width="1.2" rx="3"/>
            <text x="230" y="36" text-anchor="middle" font-weight="600" fill="#1e5e3a" font-size="10">运行时执行</text>
            <text x="230" y="52" text-anchor="middle" fill="#6b7a8f" font-size="9">Bash/Read…</text>
            <line x1="270" y1="40" x2="290" y2="40" stroke="#1e5e3a" stroke-width="1.2" marker-end="url(#ah-fc)"/>
            <rect x="290" y="15" width="80" height="50" fill="#fdfcf8" stroke="#7a5b00" stroke-width="1.2" rx="3"/>
            <text x="330" y="36" text-anchor="middle" font-weight="600" fill="#7a5b00" font-size="10">结果回灌</text>
            <text x="330" y="52" text-anchor="middle" fill="#6b7a8f" font-size="8">result → ctx</text>
            <text x="190" y="85" text-anchor="middle" fill="#6b7a8f" font-size="9">LLM 看到结果后继续推理 → 可能再次调工具 → 循环</text>
          </svg>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#function-calling #tool-use</span>
      <span class="slide-number" data-current="4" data-total="37"></span>
    </div>
    <aside class="notes">
      <p>工具的原理是 Function Calling。先说 LLM 的输入结构。每次请求，LLM 收到的就是一组 messages 和 system prompt。System prompt 是全局指令，会把身份设定、行为规则、工具的 Schema 都写在里面。messages 有 User、Assistant 和 System 三个角色，分别是用户消息、AI 回复消息和思考、系统提示词。这个消息数组就是 LLM 能看到的所有东西，上下文指的就是这些。后面整章上下文治理基本上都是围绕这件事展开的。Function Calling 就是在这个基础上约定：把工具 Schema 写进 System prompt，LLM 推理后只输出一段 tool_calls JSON，本地程序拿着 JSON 真正去执行，再把结果作为消息写回。</p>
    </aside>
  </section>

  <!-- ============ 4.1 ReAct: 概念 ============ -->
  <section class="slide" data-title="ReAct · concept">
    <div class="title-bar">
      <h2>01 · 项目与背景 — ReAct 核心循环
    </div>
    <div class="split s55">
      <div class="col-text" style="justify-content:center">
        <p class="lead">Agent 不是一次推理出全部答案。它<span class="accent">逐轮思考、调工具、读结果、再思考</span>。</p>
        <ul>
          <li><strong>Thought</strong> · 读当前上下文，决定下一步</li>
          <li><strong>Action</strong> · 输出符合 JSON Schema 的 tool call</li>
          <li><strong>Observation</strong> · 运行时执行工具，把结果序列化回灌窗口</li>
          <li><strong>Update</strong> · 窗口变大，进入下一轮 Thought</li>
        </ul>
        <p class="note">接下来六页用一个真实任务演示这个循环 —— 同时观察上下文窗口是怎么被填满的。</p>
      </div>
      <div class="col-visual">
        <div class="fig" data-fig="图 1 · ReAct 单轮循环">
          <svg viewBox="0 0 360 280" style="width:100%;height:auto;font-family:var(--font-mono);font-size:11px">
            <defs>
              <marker id="ah" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
                <path d="M0,0 L10,5 L0,10 z" fill="#1e3a8a"/>
              </marker>
            </defs>
            <!-- 四个节点 -->
            <rect x="130" y="10" width="100" height="42" fill="#fff" stroke="#1e3a8a" stroke-width="1.5"/>
            <text x="180" y="36" text-anchor="middle" fill="#0d1b2a" font-weight="600">Thought</text>

            <rect x="250" y="115" width="100" height="42" fill="#fff" stroke="#1e3a8a" stroke-width="1.5"/>
            <text x="300" y="141" text-anchor="middle" fill="#0d1b2a" font-weight="600">Action</text>

            <rect x="130" y="220" width="100" height="42" fill="#fff" stroke="#1e3a8a" stroke-width="1.5"/>
            <text x="180" y="246" text-anchor="middle" fill="#0d1b2a" font-weight="600">Observation</text>

            <rect x="10" y="115" width="100" height="42" fill="#fff" stroke="#1e3a8a" stroke-width="1.5"/>
            <text x="60" y="141" text-anchor="middle" fill="#0d1b2a" font-weight="600">Update</text>

            <!-- 箭头：Thought → Action → Observation → Update → Thought -->
            <path d="M225,52 Q280,80 285,115" stroke="#1e3a8a" stroke-width="1.3" fill="none" marker-end="url(#ah)"/>
            <path d="M295,160 Q260,200 225,222" stroke="#1e3a8a" stroke-width="1.3" fill="none" marker-end="url(#ah)"/>
            <path d="M135,225 Q90,200 75,160" stroke="#1e3a8a" stroke-width="1.3" fill="none" marker-end="url(#ah)"/>
            <path d="M75,115 Q90,80 130,52" stroke="#1e3a8a" stroke-width="1.3" fill="none" marker-end="url(#ah)"/>

            <!-- 中心白色背景遮挡箭头 -->
            <rect x="115" y="118" width="130" height="40" fill="#fff" stroke="none"/>
            <!-- 中心标签 -->
            <text x="180" y="135" text-anchor="middle" fill="#6b7a8f" font-size="10">window grows</text>
            <text x="180" y="150" text-anchor="middle" fill="#6b7a8f" font-size="10">each round</text>
          </svg>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#react #loop</span>
      <span class="slide-number" data-current="4" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>ReAct 每一轮分四步：思考、行动、观察、更新。这就是 Agent 的核心循环。</p>
    </aside>
  </section>

  <!-- ============ 4.2 ReAct frame 1: 用户提问 ============ -->
  <section class="slide" data-title="ReAct · user">
    <div class="title-bar">
      <h2>01 · 项目与背景 — ReAct 逐帧演示
    </div>
    <div class="split s64">
      <div class="col-text" style="justify-content:center">
        <p class="lead">用户的需求<span class="accent">不是机器指令</span>。Agent 需要先把它<span class="accent">翻译</span>成可执行的工具调用。</p>
        <ul>
          <li>"总结" → 需要看目录结构</li>
          <li>"可优化的模块划分" → 需要读关键源文件</li>
          <li>用户没说去哪儿看，Agent 自己定位</li>
        </ul>
        <p class="note">此时窗口里只有：System prompt + 用户这一句话。</p>
      </div>
      <div class="col-visual">
        <div class="term">
          <div class="term-bar"><span>jcli chat · session #42</span><span>~/dev/j</span></div>
          <div class="term-body">
            <div class="bubble user is-new">
              <div class="who"><span>USER</span><span class="tag">@me</span></div>
              <div class="body">总结一下 src/command/chat 目录的结构，以及可以优化的模块划分。</div>
            </div>
          </div>
        </div>
        <div class="ctx-meter">
          <span>context</span>
          <div class="track"><div class="fill" style="width:6%"></div></div>
          <span>6%</span>
        </div>
        <div class="react-state">
          <span>state:</span>
          <span class="step active">Thought</span>
          <span class="step">Action</span>
          <span class="step">Observation</span>
          <span class="step">Update</span>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#react #user-intent</span>
      <span class="slide-number" data-current="5" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>这是一个示例过程中 Agent Loop 的动画演示：用户询问"总结 chat 模块的结构和优化点"。Agent 调用了三个工具分别列目录、读入口文件、模式匹配关键定义、最后给出总结。</p>
    </aside>
  </section>

  <!-- ============ 4.3 ReAct frame 2: Agent Thought + Bash ============ -->
  <section class="slide" data-title="ReAct · action">
    <div class="title-bar">
      <h2>01 · 项目与背景 — ReAct 逐帧演示
    </div>
    <div class="split s64">
      <div class="col-text" style="justify-content:center">
        <p class="lead">Agent 先<span class="accent">列目录</span>看全貌 —— 这是面对未知项目最常见的第一步。</p>
        <ul>
          <li>选择工具 <code>Bash</code>，因为要枚举文件</li>
          <li>参数符合 JSON Schema，运行时能直接解析</li>
          <li>用 <code>ls -F</code> 区分文件与目录</li>
        </ul>
        <p class="note">注意：Agent 的"想法"也被写进上下文窗口 —— 它是后续轮次的决策依据。</p>
      </div>
      <div class="col-visual">
        <div class="term">
          <div class="term-bar"><span>jcli chat · session #42</span><span>~/dev/j</span></div>
          <div class="term-body">
            <div class="bubble user is-dim">
              <div class="who"><span>USER</span></div>
              <div class="body">总结一下 src/command/chat 目录的结构……</div>
            </div>
            <div class="bubble agent is-new">
              <div class="who"><span>AGENT</span><span class="tag">thought</span></div>
              <div class="body">用户要总结目录结构，先列出 chat/ 下的文件分布。</div>
            </div>
            <div class="bubble tool is-new">
              <div class="who"><span>TOOL CALL</span><span class="tag">Bash</span></div>
<div class="body"><span class="k">command</span>: <span class="v">ls -F src/command/chat</span>
<span class="k">description</span>: <span class="v">List chat module entries</span></div>
            </div>
          </div>
        </div>
        <div class="ctx-meter">
          <span>context</span>
          <div class="track"><div class="fill" style="width:9%"></div></div>
          <span>9%</span>
        </div>
        <div class="react-state">
          <span>state:</span>
          <span class="step done">Thought</span>
          <span class="step active">Action</span>
          <span class="step">Observation</span>
          <span class="step">Update</span>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#react #bash</span>
      <span class="slide-number" data-current="6" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>（连续翻过，不停顿。）</p>
    </aside>
  </section>

  <!-- ============ 4.4 ReAct frame 3: Bash 结果 ============ -->
  <section class="slide" data-title="ReAct · observation">
    <div class="title-bar">
      <h2>01 · 项目与背景 — ReAct 逐帧演示
    </div>
    <div class="split s64">
      <div class="col-text" style="justify-content:center">
        <p class="lead">运行时<span class="accent">在真实 shell 里执行</span> tool call，把 stdout 序列化为 tool-result 消息，写回上下文。</p>
        <ul>
          <li>12 个条目，足够建立粗略地图</li>
          <li>Agent 在下一轮能看见这些文件名</li>
          <li>结果以 <strong>文本形式</strong> 永久落在窗口里</li>
        </ul>
        <p class="note">这是 ReAct 的关键：tool 输出是会<span class="accent">长期占用 token</span> 的，不是临时变量。</p>
      </div>
      <div class="col-visual">
        <div class="term">
          <div class="term-bar"><span>jcli chat · session #42</span><span>~/dev/j</span></div>
          <div class="term-body">
            <div class="bubble agent is-dim">
              <div class="who"><span>AGENT</span><span class="tag">thought</span></div>
              <div class="body">用户要总结目录结构，先列出 chat/ 下的文件分布。</div>
            </div>
            <div class="bubble tool is-dim">
              <div class="who"><span>TOOL CALL</span><span class="tag">Bash</span></div>
              <div class="body"><span class="k">command</span>: <span class="v">ls -F src/command/chat</span></div>
            </div>
            <div class="bubble tool-result is-new">
              <div class="who"><span>TOOL RESULT</span><span class="tag">stdout</span></div>
<div class="body">agent.rs        handler/
api.rs          input_thread.rs
app.rs          markdown/
archive.rs      mod.rs
autocomplete.rs permission.rs
compact.rs      render_cache.rs
constants.rs    skill.rs
hook.rs         storage.rs
                theme.rs
                tools/
                ui/
                ui_helpers.rs</div>
            </div>
          </div>
        </div>
        <div class="ctx-meter">
          <span>context</span>
          <div class="track"><div class="fill" style="width:14%"></div></div>
          <span>14%</span>
        </div>
        <div class="react-state">
          <span>state:</span>
          <span class="step done">Thought</span>
          <span class="step done">Action</span>
          <span class="step active">Observation</span>
          <span class="step">Update</span>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#react #tool-result</span>
      <span class="slide-number" data-current="7" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>（连续翻过，不停顿。）</p>
    </aside>
  </section>

  <!-- ============ 4.5 ReAct frame 4: 下一轮 Read ============ -->
  <section class="slide" data-title="ReAct · next round">
    <div class="title-bar">
      <h2>01 · 项目与背景 — ReAct 逐帧演示
    </div>
    <div class="split s64">
      <div class="col-text" style="justify-content:center">
        <p class="lead">Agent 看完目录，决定<span class="accent">深入 app.rs</span> —— 它是名字最像入口的文件。</p>
        <ul>
          <li>选 Read 而非 Bash cat —— 结构化输出、自动行号</li>
          <li>用 offset/limit 控量，<strong>不读全文</strong></li>
          <li>结果会很长，是窗口压力的主要来源</li>
        </ul>
        <p class="note">真实 Read 一次大约带回 60 行源码（800-1500 tokens）。</p>
      </div>
      <div class="col-visual">
        <div class="term">
          <div class="term-bar"><span>jcli chat · session #42</span><span>~/dev/j</span></div>
          <div class="term-body">
            <div class="bubble tool-result is-dim">
              <div class="who"><span>TOOL RESULT</span></div>
              <div class="body">agent.rs  app.rs  compact.rs  hook.rs  ...</div>
            </div>
            <div class="bubble agent is-new">
              <div class="who"><span>AGENT</span><span class="tag">thought</span></div>
              <div class="body">app.rs 看起来是顶层入口，先读它的 struct 定义。</div>
            </div>
            <div class="bubble tool is-new">
              <div class="who"><span>TOOL CALL</span><span class="tag">Read</span></div>
<div class="body"><span class="k">file_path</span>: <span class="v">src/command/chat/app.rs</span>
<span class="k">limit</span>: <span class="v">60</span></div>
            </div>
            <div class="bubble tool-result is-new">
              <div class="who"><span>TOOL RESULT</span><span class="tag">60 lines</span></div>
<div class="body">pub struct ChatApp {
    pub ui: UIState,           // 55 fields
    pub state: ChatState,      // 7 fields
    pub tool_executor: ToolExecutor,
    pub agent: Option&lt;AgentHandle&gt;,
    pub tool_registry: Arc&lt;ToolRegistry&gt;,
    pub jcli_config: Arc&lt;JcliConfig&gt;,
    ...
}</div>
            </div>
          </div>
        </div>
        <div class="ctx-meter">
          <span>context</span>
          <div class="track"><div class="fill" style="width:24%"></div></div>
          <span>24%</span>
        </div>
        <div class="react-state">
          <span>state:</span>
          <span class="step done">Thought</span>
          <span class="step done">Action</span>
          <span class="step done">Observation</span>
          <span class="step active">Update</span>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#react #read-tool</span>
      <span class="slide-number" data-current="8" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>（连续翻过，不停顿。）</p>
    </aside>
  </section>

  <!-- ============ 4.6 ReAct frame 5: 总结 ============ -->
  <section class="slide" data-title="ReAct · summary">
    <div class="title-bar">
      <h2>01 · 项目与背景 — ReAct 逐帧演示
    </div>
    <div class="split s64">
      <div class="col-text" style="justify-content:center">
        <p class="lead">经过 <span class="accent">3 轮工具调用</span>（ls + Read app.rs + Grep "pub struct"），Agent 拼出全貌。</p>
        <ul>
          <li>窗口最终占用 <strong>38%</strong></li>
          <li>原始用户问题仅 1 行，结论却用了几千 token 才得到</li>
          <li>价值密度低 —— 大量字符是 <strong>中间产物</strong></li>
        </ul>
        <p class="note">下一节 · 上下文窗口治理：怎么让这些"中间产物"不污染长程任务。</p>
      </div>
      <div class="col-visual">
        <div class="term">
          <div class="term-bar"><span>jcli chat · session #42</span><span>round 3 done</span></div>
          <div class="term-body">
            <div class="bubble tool-result is-dim">
              <div class="who"><span>TOOL RESULT</span><span class="tag">round 2 · Read</span></div>
              <div class="body">pub struct ChatApp { ... } · 55+7+9+2 fields ...</div>
            </div>
            <div class="bubble tool-result is-dim">
              <div class="who"><span>TOOL RESULT</span><span class="tag">round 3 · Grep "pub struct"</span></div>
              <div class="body">app.rs:24 ChatApp · app.rs:108 UIState · app.rs:312 ChatState · ...</div>
            </div>
            <div class="bubble agent is-new">
              <div class="who"><span>AGENT</span><span class="tag">final</span></div>
              <div class="body">src/command/chat 共 27 个文件，按职责分四层：<br>1. <strong>入口</strong> · mod.rs / app.rs / agent.rs<br>2. <strong>状态</strong> · UIState（55 字段） + ChatState（7 字段）<br>3. <strong>I/O</strong> · handler/ · input_thread · render_cache<br>4. <strong>扩展</strong> · tools/ · skill · hook · compact<br><br>可优化点：UIState 字段过多，可按"输入/popup/缓存"再切分。</div>
            </div>
          </div>
        </div>
        <div class="ctx-meter">
          <span>context</span>
          <div class="track"><div class="fill warn" style="width:38%"></div></div>
          <span>38%</span>
        </div>
        <div class="react-state">
          <span>state:</span>
          <span class="step done">round 1</span>
          <span class="step done">round 2</span>
          <span class="step done">round 3</span>
          <span class="step active">summarize</span>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#react #close-loop</span>
      <span class="slide-number" data-current="9" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>（连续翻过，不停顿。）</p>
    </aside>
  </section>

  <!-- ============ §02 分隔页 ============ -->
  <section class="slide chapter-divider" data-title="§02 Divider">
    <span class="ch-num">Section 02</span>
    <div class="ch-title">上下文治理与状态感知</div>
    <p class="ch-sub">LLM 的窗口是物理硬上限。Agent 要长期运行，必须有一套机制来管理"往窗口里塞什么、丢掉什么"。</p>
    <aside class="notes">
      <p>（一带而过。）</p>
    </aside>
  </section>

  <!-- ============ 4. CORE: Context Window ============ -->
  <section class="slide" data-title="Context Window Challenge">
    <div class="title-bar">
      <h2>02 · 上下文治理与状态感知 — 问题与治理路径
    </div>
    <div class="split s55">
      <div class="col-text" style="justify-content:center">
        <p class="lead">上一章的 ReAct 循环已经展示：每轮工具调用的结果都会<span class="accent">永久占用</span>上下文 token。轮次越多，窗口越满，Agent 开始幻觉、重复操作、忘记目标。</p>
        <h4>两条治理路径</h4>
        <ul>
          <li><strong>分级压缩</strong> · micro 快速替换旧工具结果为占位符，auto 调用 LLM 做深度摘要。均配备<strong>豁免机制</strong>保护关键信息。</li>
          <li><strong>优先级窗口</strong> · 不是固定取最近 N 条，而是给消息打价值分，按 3:6:1 比例填充，保证方向性信息优先入窗。实测可稳定运行 <strong>2000+ 轮</strong>。</li>
        </ul>
      </div>
      <div class="col-visual">
        <div class="fig" data-fig="图 · 窗口压力来源与两条治理路径">
          <svg viewBox="0 0 380 320" style="width:100%;height:auto;font-family:var(--font-mono);font-size:11px">
            <defs>
              <marker id="ah-cw" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
                <path d="M0,0 L10,5 L0,10 z" fill="#1e3a8a"/>
              </marker>
              <linearGradient id="grad-fill" x1="0" y1="0" x2="1" y2="0">
                <stop offset="0%" stop-color="#1e3a8a"/>
                <stop offset="60%" stop-color="#7a5b00"/>
                <stop offset="100%" stop-color="#7a1f1f"/>
              </linearGradient>
            </defs>

            <!-- 顶部窗口条 -->
            <text x="20" y="22" font-weight="600" fill="#0d1b2a" font-size="12">Context Window · 128K tokens</text>
            <rect x="20" y="32" width="340" height="22" fill="#f3f5f9" stroke="#e5e9f2" stroke-width="1"/>
            <rect x="20" y="32" width="306" height="22" fill="url(#grad-fill)" opacity="0.85"/>
            <text x="338" y="48" fill="#7a1f1f" font-size="10" text-anchor="end">90%+ 危险区</text>

            <!-- ReAct 引出 -->
            <text x="20" y="82" font-weight="600" fill="#0d1b2a" font-size="11">ReAct 循环的必然结果</text>
            <text x="20" y="100" fill="#6b7a8f" font-size="10">每轮 Thought → Action → Observation 都在消耗窗口</text>
            <text x="20" y="116" fill="#7a1f1f" font-size="10" font-weight="600">工具结果是主要膨胀源，且价值衰减极快</text>

            <!-- 两条路径 -->
            <text x="20" y="148" font-weight="600" fill="#0d1b2a" font-size="11">两条治理路径</text>
            <g transform="translate(20,158)">
              <rect width="160" height="40" fill="#fbfdfb" stroke="#1e5e3a" stroke-width="1.2"/>
              <text x="10" y="16" font-size="10" fill="#1e5e3a" font-weight="600">① 分级压缩</text>
              <text x="10" y="30" font-size="9" fill="#6b7a8f">micro 快速替换 / auto 深度摘要</text>
            </g>
            <g transform="translate(200,158)">
              <rect width="160" height="40" fill="#f5f8ff" stroke="#1e3a8a" stroke-width="1.2"/>
              <text x="10" y="16" font-size="10" fill="#1e3a8a" font-weight="600">② 优先级窗口</text>
              <text x="10" y="30" font-size="9" fill="#6b7a8f">3:6:1 价值比例填充</text>
            </g>

            <!-- 共同机制 -->
            <text x="20" y="222" font-weight="600" fill="#0d1b2a" font-size="11">共同机制</text>
            <g transform="translate(20,232)">
              <rect width="340" height="26" fill="#fbf8f0" stroke="#7a5b00" stroke-width="1.2"/>
              <text x="10" y="17" font-size="10" fill="#7a5b00" font-weight="600">豁免机制 · 保护高价值消息不被压缩失真</text>
            </g>
            <g transform="translate(20,266)">
              <rect width="340" height="26" fill="#f8f8fb" stroke="#1e3a8a" stroke-width="1.2"/>
              <text x="10" y="17" font-size="10" fill="#1e3a8a" font-weight="600">动态 System Prompt · 让 Agent 感知持续状态</text>
            </g>
          </svg>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>ReAct 的本质决定了窗口必然会被工具结果不断挤占；上下文到达高水位，模型出幻觉、服务不可用的概率大大提高。我们做了三个方向来治理上下文：分级压缩、优先级窗口、动态状态感知。</p>
    </aside>
  </section>

  <!-- ============ 5. Compaction System ============ -->
  <section class="slide" data-title="Compaction System">
    <div class="title-bar">
      <h2>02 · 上下文治理与状态感知 — 分级压缩与豁免机制
    </div>
    <div class="split s55">
      <div class="col-text" style="overflow:hidden">
        <h4 style="margin-top:0;margin-bottom:2px">Micro 压缩 · 快速替换</h4>
        <p style="margin-bottom:2px">将久远的工具调用结果<strong>替换为占位符</strong>，仅保留"调用了什么"而丢弃内容。速度快、token 释放量大，适合窗口接近上限时紧急腾空间。</p>
        <h4 style="margin-bottom:2px">Auto 压缩 · 深度摘要</h4>
        <p style="margin-bottom:2px">调用 LLM 将对话历史总结为精简摘要，然后<strong>清空窗口 + 写入摘要</strong>。成本较高但压缩彻底，适合 Agent 运行很久需要"大扫除"的场景。</p>
        <h4 style="margin-bottom:2px">豁免机制 · 保护关键信息</h4>
        <p style="margin-bottom:2px">无论 micro 还是 auto，以下消息<strong>不被压缩</strong>：</p>
        <ul style="line-height:1.5;margin:0;padding-left:18px">
          <li>用户消息 · 需求源头</li>
          <li>Agent 纯文本回复 · 决策记录</li>
          <li>TodoWrite / Plan / Skill · 方向性信息</li>
          <li>SubAgent 最终结果 · 调研结论</li>
        </ul>
      </div>
      <div class="col-visual">
        <h4 style="margin-top:0;font-size:14px;color:var(--text-3)">工具结果的价值衰减</h4>
        <table class="vis-table">
          <tr><th>工具类型</th><th>价值判断</th><th>豁免</th></tr>
          <tr><td>Read</td><td class="no">低 · 文件内容随编辑失效</td><td class="no">No</td></tr>
          <tr><td>Bash</td><td class="no">低 · 输出过时极快</td><td class="no">No</td></tr>
          <tr><td>Edit</td><td class="no">低 · diff 已被后续覆盖</td><td class="no">No</td></tr>
          <tr><td>Write</td><td class="no">低 · 与 Edit 同理</td><td class="no">No</td></tr>
          <tr><td>Grep</td><td class="no">低 · 搜索结果一次性</td><td class="no">No</td></tr>
          <tr><td>TodoWrite</td><td class="yes">高 · 任务追踪</td><td class="yes">Yes</td></tr>
          <tr><td>Plan</td><td class="yes">高 · 执行方案</td><td class="yes">Yes</td></tr>
          <tr><td>Ask</td><td class="yes">高 · 用户意图确认</td><td class="yes">Yes</td></tr>
          <tr><td>Skill</td><td class="yes">高 · 能力定义</td><td class="yes">Yes</td></tr>
        </table>
        <p class="dim" style="font-size:14px;margin-top:8px;line-height:1.6">Bash / Edit / Read / Write 的信息价值随轮次衰减极快——第 20 轮时，第 3 轮的 Read 结果对决策几乎无价值。</p>
      </div>
    </div>
    <aside class="notes">
      <p>压缩分两级：micro 把较早轮次的工具调用换成占位符，程序化操作，快且可控，每小轮 Agent Loop 触发；auto 调 LLM 把整段历史摘要后清空再写回，彻底但贵且不可控，只在高水位触发。这里有一个设计是豁免机制：用户消息、Plan、TodoWrite、SubAgent 结论这些方向性消息不参与压缩，只压缩 Read、Bash、Edit 这种衰减极快的执行结果。</p>
    </aside>
  </section>

  <!-- ============ 5.5. PRIORITY FUNNEL ============ -->
  <section class="slide" data-title="Priority Funnel">
    <div class="title-bar">
      <h2>02 · 上下文治理与状态感知 — 优先级窗口：3:6:1 价值比例填充
    </div>
    <div class="split s55">
      <div class="col-text">
        <p class="lead">固定取最近 N 条消息的问题：<strong>方向性信息可能在很早的位置</strong>，被新消息挤出窗口。我们把上下文当作推荐系统——给消息打价值分，优先填高价值的。</p>
        <h4>高价值消息类型</h4>
        <ul>
          <li><strong>用户消息</strong> · 需求源头，方向锚点</li>
          <li><strong>TodoWrite</strong> · 当前任务追踪</li>
          <li><strong>Clean</strong>（Plan clear-and-approve）· 执行方案</li>
          <li><strong>Skill 加载</strong> · 能力定义</li>
          <li><strong>Agent 纯文本回复</strong> · 决策记录</li>
        </ul>
        <h4>3:6:1 填充比例</h4>
        <p>在固定窗口内，按 3:6:1 比例分别加载<strong>高价值 / 中价值 / 低价值</strong>消息。保证 Agent 在长程运行时始终能看到方向性信息。</p>
        <p class="note">实测：可稳定运行 <strong>2000+ 轮</strong>消息持续任务，Agent 方向性保持稳定。</p>
      </div>
      <div class="col-visual">
        <div class="priority-funnel" style="margin-top:0">
          <div class="lane">
            <h5>候选池 (按价值打分)</h5>
            <div class="msg-chip" data-pri="high">用户初始需求 <span class="score">p=1.00</span></div>
            <div class="msg-chip" data-pri="high">Plan 文档 <span class="score">p=0.95</span></div>
            <div class="msg-chip" data-pri="high">Agent 决策语句 <span class="score">p=0.90</span></div>
            <div class="msg-chip" data-pri="high">TODO 当前任务 <span class="score">p=0.88</span></div>
            <div class="msg-chip" data-pri="high">SubAgent 调研结论 <span class="score">p=0.85</span></div>
            <div class="msg-chip" data-pri="mid">Skill 加载内容 <span class="score">p=0.70</span></div>
            <div class="msg-chip" data-pri="low">远期 Bash 输出 <span class="score">p=0.20</span></div>
            <div class="msg-chip" data-pri="low">中间 Grep 结果 <span class="score">p=0.15</span></div>
            <div class="msg-chip" data-pri="low">已被覆盖的旧 Edit <span class="score">p=0.08</span></div>
          </div>
          <div class="sorter">
            <span class="lbl">SORT BY<br>VALUE</span>
            <span class="arr">→</span>
            <span class="lbl">FILL<br>3:6:1</span>
          </div>
          <div class="lane">
            <h5>装入窗口（3:6:1 比例）</h5>
            <div class="msg-chip" data-pri="high">用户初始需求 <span class="score">30%</span></div>
            <div class="msg-chip" data-pri="high">Plan 文档 <span class="score">30%</span></div>
            <div class="msg-chip" data-pri="high">Agent 决策语句 <span class="score">30%</span></div>
            <div class="msg-chip" data-pri="high">TODO 当前任务 <span class="score">30%</span></div>
            <div class="msg-chip" data-pri="high">SubAgent 调研结论 <span class="score">30%</span></div>
            <div class="msg-chip" data-pri="mid">Skill 加载内容 <span class="score">60%</span></div>
            <div class="msg-chip" data-pri="low">远期 Bash 输出 <span class="score">10%</span></div>
            <div class="msg-chip" data-pri="low">中间 Grep 结果 <span class="score">DROP</span></div>
            <div class="msg-chip" data-pri="low">已被覆盖的旧 Edit <span class="score">DROP</span></div>
          </div>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>传统的滑动窗口固定取最近 N 条消息，这么做导致方向性消息容易被后来的执行消息挤出窗口。所以给不同消息打上不同的价值再按 3:6:1 的比例填窗——30% 给方向性消息、60% 给近期上下文、10% 给执行细节。和背包问题不同的是不能完全没有低价值的执行消息，否则 AI 容易凭空猜测不调用工具。</p>
    </aside>
  </section>

  <!-- ============ 6. Dynamic System Prompt ============ -->
  <section class="slide" data-title="Dynamic System Prompt">
    <div class="title-bar">
      <h2>02 · 上下文治理与状态感知 — 动态状态感知：两层注入机制
    </div>
    <div class="split s55">
      <div class="col-text" style="justify-content:center">
        <h4 style="margin-top:0">问题</h4>
        <p>Agent 每轮推理只能看到上下文窗口内的内容。一旦信息被压缩或滚出窗口，Agent 就<strong>失去感知</strong>——忘记后台任务、忽略待办、遗漏 Teammate 消息。</p>
        <h4>两层注入</h4>
        <ul style="line-height:1.7;margin:0;padding-left:18px">
          <li><strong>System Prompt 占位符</strong> · 模板中 <code>{{.background_tasks}}</code>、<code>{{.teammates}}</code> 等在<strong>每轮请求前</strong>替换为实时状态快照</li>
          <li><strong>&lt;system-reminder&gt; 消息</strong> · 突发事件（后台任务完成 / TODO 过期 / Teammate 新消息）以 User 角色<strong>按需注入</strong></li>
        </ul>
        <p class="note" style="margin-top:4px">软提醒，不强打断——Agent 在下一轮推理时自然看到，可暂时忽略处理更紧急的事。</p>
      </div>
      <div class="col-visual">
        <div class="arch" style="margin-top:0;width:100%;gap:8px;font-size:13px">
          <div class="tier" style="gap:8px"><div class="tname" style="padding:8px;font-size:11px">静态基础</div>
            <div class="cells" style="gap:6px"><div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">项目规范</h4><p style="font-size:11px;margin:0">AGENTS.md</p></div>
              <div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">工具定义</h4><p style="font-size:11px;margin:0">Tool Schema</p></div>
              <div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">环境信息</h4><p style="font-size:11px;margin:0">OS / Shell</p></div>
              <div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">Skill</h4><p style="font-size:11px;margin:0">按需加载</p></div>
            </div>
          </div>
          <div class="tier hl" style="gap:8px"><div class="tname" style="padding:8px;font-size:11px">Layer 1 · System Prompt 占位符</div>
            <div class="cells" style="gap:6px"><div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">后台任务</h4><p style="font-size:11px;margin:0">Task / BackgroundJob</p></div>
              <div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">Teammate</h4><p style="font-size:11px;margin:0">协作队友状态</p></div>
              <div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">TODO</h4><p style="font-size:11px;margin:0">过期未更新提醒</p></div>
              <div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">Session</h4><p style="font-size:11px;margin:0">交互式任务</p></div>
            </div>
          </div>
          <div class="tier" style="border-top:2px solid var(--accent);background:#fdfcf8;gap:8px"><div class="tname" style="color:var(--accent);padding:8px;font-size:11px">Layer 2 · system-reminder 消息</div>
            <div class="cells" style="gap:6px"><div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">任务完成</h4><p style="font-size:11px;margin:0">background_task_completed</p></div>
              <div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">TODO 过期</h4><p style="font-size:11px;margin:0">todo_reminder</p></div>
              <div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">队友消息</h4><p style="font-size:11px;margin:0">teammate 新消息</p></div>
              <div class="cell" style="padding:10px 8px"><h4 style="font-size:13px;margin:0 0 2px">Skill 重新注入</h4><p style="font-size:11px;margin:0">invoked_skills</p></div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>Agent 还需要知道"窗口外正在发生什么"。这里做了 System Prompt 和 messages 两层注入。前者占位符每轮替换为后台任务、队友状态等实时快照；后者则处理通知提醒事件，用特殊的 XML 标签包裹消息写入，不打断，Agent 在下一次构建 messages 数组时自然看到。</p>
    </aside>
  </section>

  <!-- ============ 6.5. 长程运行实测 ============ -->
  <section class="slide" data-title="Long Running Proof">
    <div class="title-bar">
      <h2>02 · 上下文治理与状态感知 — 治理成果：长程运行实测
    </div>
    <div class="split s55">
      <div class="col-text" style="gap:6px;justify-content:center">
        <h4 style="margin-top:0">三重保障的协同效果</h4>
        <ul style="line-height:1.7;margin:0">
          <li><strong>分级压缩</strong> · micro 快速替换旧工具结果，auto 深度摘要清空窗口</li>
          <li><strong>优先级窗口</strong> · 3:6:1 比例保证方向性信息始终在窗</li>
          <li><strong>动态 System Prompt</strong> · 压缩后仍感知持续性事件</li>
        </ul>
        <h4 style="margin-top:2px">指数退避 · API 鲁棒性</h4>
        <p>LLM API 限流、超时由指数退避 + 随机等待自动恢复，不打断工作流。</p>
        <p class="dim" style="font-size:14px">使用场景：睡前审批 Plan → Agent 自主执行 → 早上收到可运行原型。</p>
      </div>
      <div class="col-visual">
        <div class="fig-shot" style="display:flex;flex-direction:column;overflow:hidden">
          <img src="pics/long-running-session-context.jpg" alt="2000+ 消息长程运行截图" style="flex:1;min-height:0;object-fit:cover;object-position:left top">
          <div class="shot-caption">实测：2000+ 轮消息，上下文始终健康，方向性不丢失</div>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>上述措施配合指数退避随机等待来处理 API 故障和限频，可稳定跑到 2000 多轮消息，窗口始终维持在健康水平、方向不跑偏。这里只是取了运行比较长久的一个记录，实际上限会更高。</p>
    </aside>
  </section>

  <!-- ============ §03 分隔页 ============ -->
  <section class="slide chapter-divider" data-title="§03 Divider">
    <span class="ch-num">Section 03</span>
    <div class="ch-title">面向场景强化的工具</div>
    <p class="ch-sub">每个工具的引入都是为了解决一个具体问题——Grep 构建上下文、Ask 防漂移、Plan 先调研再执行、Edit 精确替换、执行三态自动流转。</p>
    <aside class="notes">
      <p>（一带而过。）</p>
    </aside>
  </section>

  <!-- ============ 7. TOOL SYSTEM OVERVIEW ============ -->
  <section class="slide" data-title="Tool System">
    <div class="title-bar">
      <h2>03 · 面向场景强化的工具 — 从最小 Agent 到渐进增强
    </div>
    <div class="split s55">
      <div class="col-text" style="justify-content:center">
        <h4 style="margin-top:0">最小 Agent：4 个工具即可工作</h4>
        <p>Bash + Read + Write + LoadSkill — 能读文件、写文件、执行命令、按需加载技能。<strong>理论上够用。</strong></p>
        <h4 style="color:var(--bad)">问题：基础工具覆盖不了的场景</h4>
        <ul>
          <li>面对未知项目，只能 <code>Bash grep</code> 搜索 → 匹配质量不稳定、格式难解析</li>
          <li>用户需求模糊 → 盲目执行，产物与预期<strong>漂移</strong></li>
          <li>LLM 没想清楚就开始改 → 变更不完整，调研占满窗口</li>
          <li>长任务跑着跑着忘记目标 → 漏改漏做</li>
        </ul>
        <p>每个增强工具都是为了<strong>解决一个具体问题</strong>。</p>
      </div>
      <div class="col-visual">
        <p style="margin:0 0 4px;font-size:11px;color:var(--text-3);font-family:var(--font-mono)">基础层 = 最小 Agent &nbsp;|&nbsp; 增强层 ← 每个工具解决一个具体问题</p>
        <div class="arch" style="margin-top:0">
          <div class="tier"><div class="tname">基础层</div>
            <div class="cells"><div class="cell"><h4>Read</h4><p style="white-space:nowrap">读取文件</p></div>
              <div class="cell"><h4>Write</h4><p style="white-space:nowrap">写入文件</p></div>
              <div class="cell"><h4>Bash</h4><p style="white-space:nowrap">执行命令</p></div>
              <div class="cell"><h4>LoadSkill</h4><p style="white-space:nowrap">按需加载</p></div>
            </div>
          </div>
          <div class="tier hl">
            <div class="tname">增强层</div>
            <div class="cells"><div class="cell"><h4>Grep</h4><p style="white-space:nowrap">模板匹配</p></div>
              <div class="cell"><h4>Ask</h4><p style="white-space:nowrap">澄清意图</p></div>
              <div class="cell"><h4>Plan</h4><p style="white-space:nowrap">先调研再执行</p></div>
              <div class="cell"><h4>Web</h4><p style="white-space:nowrap">网络搜索</p></div>
            </div>
          </div>
          <div class="tier"><div class="tname">协作层</div>
            <div class="cells"><div class="cell"><h4>Todo</h4><p style="white-space:nowrap">任务追踪</p></div>
              <div class="cell"><h4>SubAgent</h4><p style="white-space:nowrap">委派调研</p></div>
              <div class="cell"><h4>Teammate</h4><p style="white-space:nowrap">多人协作</p></div>
              <div class="cell"><h4>Session</h4><p style="white-space:nowrap">交互会话</p></div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>有 Read、Write、Bash、LoadSkill 这几个工具 Agent 便能工作。工具不是越多越好。本次实现面向频繁场景，适当增加增强一些工具。下面选三个典型展开。</p>
    </aside>
  </section>

  <!-- ============ 9. Plan + approve ============ -->
  <section class="slide" data-title="Plan · approve">
    <div class="title-bar">
      <h2>03 · 面向场景强化的工具 — Plan 调研 + 审批清空上下文
    </div>
    <div class="split s55">
      <div class="col-text">
        <h4 style="margin-top:0;margin-bottom:2px">Plan — 先调研再执行</h4>
        <p style="margin-bottom:2px"><strong style="color:var(--bad)">问题：</strong>LLM 边想边改，调研读 30 个文件占满窗口 → 改一处忘一处。</p>
        <p style="margin-bottom:4px"><strong style="color:var(--good)">方案：</strong>Plan 模式 → 只读调研 → plan.md → 审批 → <strong>清空上下文</strong> → 按计划执行。</p>
        <h4 style="margin-bottom:2px">审批三选项</h4>
        <ul style="line-height:1.6;margin:0 0 6px;padding-left:18px">
          <li><strong>① 批准</strong> — 保留上下文，开始实施</li>
          <li style="color:var(--good)"><strong>② 批准并清空上下文</strong> — 仅保留 plan.md，丢弃调研产物 ← 推荐</li>
          <li><strong>③ 驳回</strong> — 留在 Plan Mode 修改</li>
        </ul>
        <h4 style="margin-bottom:2px">窗口回收效果</h4>
        <p>调研阶段大量工具结果（Read / Grep / Bash）在执行阶段<strong>全部丢弃</strong>，仅保留一份 plan.md（~1.2K tokens）。窗口从 <strong>65% → 12%</strong>，执行阶段拥有充足空间。</p>
      </div>
      <div class="col-visual">
        <svg viewBox="0 0 440 250" style="width:100%;height:auto;font-family:var(--font-mono);font-size:10px">
          <defs>
            <marker id="pa1" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><path d="M0,0 L10,5 L0,10 z" fill="#1e3a8a"/></marker>
          </defs>
          <!-- Plan 流程 -->
          <text x="220" y="14" text-anchor="middle" font-weight="600" fill="var(--text-1)" font-size="12">Plan 工作流</text>
          <rect x="10" y="28" width="110" height="60" fill="#f5f8ff" stroke="#1e3a8a" stroke-width="1.5" rx="3"/>
          <text x="65" y="48" text-anchor="middle" font-weight="600" fill="#0d1b2a" font-size="11">① 调研</text>
          <text x="65" y="64" text-anchor="middle" fill="#6b7a8f" font-size="9">Read · Grep · Glob</text>
          <text x="65" y="78" text-anchor="middle" fill="#7a1f1f" font-size="9">window: 60%</text>
          <path d="M120,58 L150,58" stroke="#1e3a8a" stroke-width="1.3" marker-end="url(#pa1)"/>

          <rect x="155" y="28" width="120" height="60" fill="#fdfcf8" stroke="#7a5b00" stroke-width="1.5" rx="3"/>
          <text x="215" y="48" text-anchor="middle" font-weight="600" fill="#0d1b2a" font-size="11">② plan.md</text>
          <text x="215" y="64" text-anchor="middle" fill="#6b7a8f" font-size="9">自包含执行计划</text>
          <text x="215" y="78" text-anchor="middle" fill="#7a5b00" font-size="9">用户审批三选一</text>

          <!-- CLEAR 标记 -->
          <line x1="290" y1="95" x2="290" y2="65" stroke="#7a1f1f" stroke-width="1.2" stroke-dasharray="3,2"/>
          <text x="290" y="110" text-anchor="middle" fill="#7a1f1f" font-weight="700" font-size="10">CLEAR ↑</text>
          <text x="290" y="124" text-anchor="middle" fill="#7a1f1f" font-size="9">调研窗口丢弃</text>

          <rect x="315" y="28" width="115" height="60" fill="#fbfdfb" stroke="#1e5e3a" stroke-width="1.5" rx="3"/>
          <text x="372" y="48" text-anchor="middle" font-weight="600" fill="#0d1b2a" font-size="11">③ 执行</text>
          <text x="372" y="64" text-anchor="middle" fill="#6b7a8f" font-size="9">Edit · Write · Test</text>
          <text x="372" y="78" text-anchor="middle" fill="#1e5e3a" font-size="9">window: 12%</text>
          <path d="M275,58 L310,58" stroke="#1e3a8a" stroke-width="1.3" marker-end="url(#pa1)"/>

          <!-- 下方效果对比 -->
          <line x1="10" y1="148" x2="430" y2="148" stroke="#e5e9f2" stroke-width="1"/>
          <text x="220" y="168" text-anchor="middle" font-weight="600" fill="var(--text-1)" font-size="11">窗口回收</text>

          <!-- 调研阶段 -->
          <rect x="10" y="182" width="200" height="24" fill="#fef3f3" stroke="#7a1f1f" stroke-width="1" rx="2"/>
          <text x="110" y="198" text-anchor="middle" fill="#7a1f1f" font-size="9">调研阶段：30+ 工具结果 · window 65%</text>

          <!-- CLEAR 箭头 -->
          <path d="M210,194 L225,194" stroke="#7a1f1f" stroke-width="1.3" marker-end="url(#pa1)"/>

          <!-- 执行阶段 -->
          <rect x="230" y="182" width="200" height="24" fill="#f0fff4" stroke="#1e5e3a" stroke-width="1" rx="2"/>
          <text x="330" y="198" text-anchor="middle" fill="#1e5e3a" font-size="9">执行阶段：plan.md · window 12%</text>

          <text x="220" y="232" text-anchor="middle" fill="var(--text-3)" font-size="9">丢弃调研中间产物 · 保留方向性计划 · 窗口干净执行</text>
        </svg>
      </div>
    </div>
    <aside class="notes">
      <p>第一个问题是 LLM 边想边改、调研读 30 个文件就把窗口撑爆。我的做法是 Plan 模式：调研阶段只给只读工具、产出 plan.md，用户审批的时候顺便清空上下文——把几十 K 的调研产物丢掉、只保留 plan。窗口能从 65% 直接回到 12%。</p>
    </aside>
  </section>

  <section class="slide" data-title="Plan · execute">
    <div class="title-bar">
      <h2>03 · 面向场景强化的工具 — 清空窗口按计划执行
    </div>
    <div class="split s55">
      <div class="col-text" style="gap:4px;overflow:hidden;display:flex;flex-direction:column">
        <p class="lead" style="font-size:18px">窗口从 65% <span class="accent">回到 12%</span>：调研产生的 30+ 工具结果全部丢弃。</p>
        <ul style="line-height:1.7;margin:0">
          <li>Agent 重新启动，上下文只含 plan.md</li>
          <li>工具集恢复为<strong>完整集</strong>（Edit / Write / Bash）</li>
          <li>不再有调研中间产物干扰决策</li>
        </ul>
        <p class="note" style="margin-top:2px;padding-top:4px"><span class="accent">clear & approve</span> 机制：忘掉调研细节 → 窗口干净，错误率下降</p>
        <div class="fig-shot" style="margin-top:4px;flex:1;min-height:0;overflow:hidden;display:flex;flex-direction:column">
          <img src="pics/plan-mode-clear-and-approval.png" alt="plan-mode 真实截图" style="flex:1;min-height:0;object-fit:cover;object-position:left top">
          <div class="shot-caption">jcli plan-mode · 批准 / 清空上下文 / 驳回三选项</div>
        </div>
      </div>
      <div class="col-visual">
        <div class="term">
          <div class="term-bar"><span>execute-mode · session #78 (new ctx)</span><span>full tools</span></div>
          <div class="term-body">
            <div class="bubble agent is-new">
              <div class="who"><span>SYSTEM</span><span class="tag">context cleared</span></div>
              <div class="body">上下文已清空。仅保留 plan.md (~1.2K tokens)。可使用完整工具集。</div>
            </div>
            <div class="bubble agent is-new">
              <div class="who"><span>AGENT</span><span class="tag">step 1/5</span></div>
              <div class="body">按 plan.md step 1：创建 ui_state.rs。</div>
            </div>
            <div class="bubble tool">
              <div class="who"><span>TOOL CALL</span><span class="tag">Write</span></div>
<div class="body"><span class="k">file_path</span>: src/command/chat/ui_state.rs
<span class="k">content</span>: pub struct UIState { /* 55 fields */ }</div>
            </div>
            <div class="bubble tool-result">
              <div class="who"><span>TOOL RESULT</span></div>
              <div class="body">created 89 lines</div>
            </div>
            <div class="bubble agent is-new">
              <div class="who"><span>AGENT</span><span class="tag">step 2/5</span></div>
              <div class="body">step 1 done. 进行 step 2：创建 chat_state.rs。</div>
            </div>
          </div>
        </div>
        <div class="ctx-meter">
          <span>context</span>
          <div class="track"><div class="fill" style="width:12%"></div></div>
          <span>12% · clean window</span>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#plan #clear-context</span>
      <span class="slide-number" data-current="17" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>清空之后，Agent 重新启动、工具集恢复完整，就可以对照 plan 一步步实施。代价是 Agent 忘掉了调研细节，但收益是执行阶段窗口干净，错误率明显下降。</p>
    </aside>
  </section>

  <!-- ============ 9.5. Edit — 精确替换 ============ -->
  <section class="slide" data-title="Edit · replace">
    <div class="title-bar">
      <h2>03 · 面向场景强化的工具 — Edit：精确替换与凭证确认的批量机制
    </div>
    <div class="split s55">
      <div class="col-text">
        <h4 style="margin-top:0;margin-bottom:2px">精确字符串匹配</h4>
        <p style="margin-bottom:6px">基于 <code>old_string</code> 在文件中做<strong>精确匹配</strong>，替换为 <code>new_string</code>。只发送变更部分，不触碰文件其他内容。匹配失败时返回<strong>诊断提示</strong>（缩进、空白、行尾差异），帮助快速定位问题。</p>
        <h4 style="margin-bottom:2px">唯一性约束</h4>
        <p style="margin-bottom:6px"><code>old_string</code> 在文件中必须<strong>唯一匹配</strong>。匹配多次时返回带上下文的预览，提示加更多上下文使其唯一。</p>
        <h4 style="margin-bottom:2px">批量替换 · 凭证确认机制</h4>
        <p>当 <code>replace_all=true</code> 时，基于<strong>当前文件内容生成摘要凭证</strong>，强制先预览再执行：</p>
        <ul style="line-height:1.6;margin:0;padding-left:18px">
          <li><strong>① 预览</strong> — 返回全部匹配位置 + <code>confirm_token</code>（由文件内容摘要生成）</li>
          <li><strong>② 确认</strong> — 回传 token 才执行替换。文件内容变化 → 摘要不匹配 → 凭证失效 → 强制重新预览</li>
        </ul>
        <p class="note" style="margin-top:4px">凭证绑定文件快照，AI 无法预先伪造——必须先看到真实匹配才能拿到凭证。</p>
      </div>
      <div class="col-visual">
        <svg viewBox="0 0 440 280" style="width:100%;height:auto;font-family:var(--font-mono);font-size:10px">
          <defs>
            <marker id="ed1" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><path d="M0,0 L10,5 L0,10 z" fill="#1e3a8a"/></marker>
          </defs>

          <!-- 精确匹配示意 -->
          <text x="220" y="14" text-anchor="middle" font-weight="600" fill="var(--text-1)" font-size="12">精确字符串匹配</text>
          <text x="10" y="36" font-weight="600" fill="#1e5e3a" font-size="10">Edit 操作</text>
          <rect x="10" y="42" width="180" height="22" fill="#fefdf5" stroke="#7a5b00" stroke-width="1" rx="2"/>
          <text x="100" y="57" text-anchor="middle" fill="#7a5b00" font-size="9">old_string (精确匹配)</text>
          <path d="M190,53 L210,53" stroke="#1e5e3a" stroke-width="1.3" marker-end="url(#ed1)"/>
          <rect x="215" y="42" width="180" height="22" fill="#f0fff4" stroke="#1e5e3a" stroke-width="1" rx="2"/>
          <text x="305" y="57" text-anchor="middle" fill="#1e5e3a" font-size="9">new_string (替换)</text>
          <text x="220" y="82" text-anchor="middle" fill="var(--text-3)" font-size="9">匹配失败 → 诊断提示 · 匹配多次 → 建议加上下文</text>

          <!-- 分隔线 -->
          <line x1="10" y1="98" x2="430" y2="98" stroke="#e5e9f2" stroke-width="1"/>

          <!-- 批量替换凭证确认 -->
          <text x="220" y="118" text-anchor="middle" font-weight="600" fill="var(--text-1)" font-size="11">批量替换 · replace_all 凭证确认</text>

          <!-- 文件摘要 -->
          <rect x="10" y="132" width="120" height="34" fill="#f5f8ff" stroke="#1e3a8a" stroke-width="1.5" rx="3"/>
          <text x="70" y="148" text-anchor="middle" font-weight="600" fill="#0d1b2a" font-size="10">当前文件</text>
          <text x="70" y="162" text-anchor="middle" fill="#1e3a8a" font-size="9">digest(file_content)</text>

          <path d="M130,149 L148,149" stroke="#1e3a8a" stroke-width="1.3" marker-end="url(#ed1)"/>

          <!-- 阶段1: 预览 -->
          <rect x="152" y="128" width="130" height="60" fill="#f5f8ff" stroke="#1e3a8a" stroke-width="1.5" rx="3"/>
          <text x="217" y="146" text-anchor="middle" font-weight="600" fill="#0d1b2a" font-size="10">① 预览</text>
          <text x="217" y="160" text-anchor="middle" fill="#6b7a8f" font-size="9">返回全部匹配</text>
          <text x="217" y="174" text-anchor="middle" fill="#1e3a8a" font-size="9">+ confirm_token</text>

          <path d="M282,158 L300,158" stroke="#1e3a8a" stroke-width="1.3" marker-end="url(#ed1)"/>

          <!-- 阶段2: 确认 -->
          <rect x="304" y="128" width="126" height="60" fill="#fbfdfb" stroke="#1e5e3a" stroke-width="1.5" rx="3"/>
          <text x="367" y="146" text-anchor="middle" font-weight="600" fill="#0d1b2a" font-size="10">② 确认执行</text>
          <text x="367" y="160" text-anchor="middle" fill="#6b7a8f" font-size="9">回传 token</text>
          <text x="367" y="174" text-anchor="middle" fill="#1e5e3a" font-size="9">校验 → 替换</text>

          <!-- 失效路径 -->
          <text x="220" y="210" text-anchor="middle" fill="#7a1f1f" font-size="9">文件变化 → 摘要不匹配 → token 失效 → 重新预览</text>

          <!-- 安全说明 -->
          <rect x="10" y="224" width="420" height="28" fill="#fbf8f0" stroke="#7a5b00" stroke-width="1" rx="2"/>
          <text x="220" y="242" text-anchor="middle" fill="#7a5b00" font-size="9">凭证由文件内容摘要生成 · AI 无法预先伪造 · 必须先看真实匹配才能拿到</text>
        </svg>
      </div>
    </div>
    <aside class="notes">
      <p>Edit 工具我做了两个比较关键的设计。一是精确字符串匹配——基于 old_string 在文件中唯一匹配，匹配失败会给出诊断、匹配多次会提示加上下文。二是批量替换的凭证握手：replace_all 不直接执行，先返回预览和文件摘要凭证，AI 把凭证回传才执行；文件一变凭证就失效，AI 也没法预先伪造，因为凭证依赖它必须先看到的真实匹配结果。</p>
    </aside>
  </section>

  <!-- ============ 10. BASH / SESSION / BACKGROUND ============ -->
  <!-- ============ 10a. 执行三态 · 概念 ============ -->
  <section class="slide" data-title="执行三态 · concept">
    <div class="title-bar">
      <h2>03 · 面向场景强化的工具 — 执行三态概览
    </div>
    <div class="split s55">
      <div class="col-text">
        <h4 style="margin-top:0;color:var(--bad)">问题背景 · 一个真实的卡死案例</h4>
        <p style="margin-bottom:6px">生成业务软件时，Agent 执行了 <code>npm create vite@latest my-app -- --template react-ts</code>。命令<strong>久久没有返回</strong></p>
        <p style="margin-bottom:8px">排查发现：这个命令有<strong>交互式设计</strong>，要求用户输入 Enter 确认安装。但 Agent 用的是同步 Bash —— 进程在等 stdin，Agent 在等 stdout，<strong>双方互相等待</strong>，形成死锁。</p>
        <p class="dim" style="font-size:14px;margin-bottom:8px">这类交互式命令在包管理器中很常见：npm init、pip install、apt-get、Homebrew……Agent 必须能识别并正确处理。</p>
        <h4 style="margin-top:10px">设计目标</h4>
        <p style="margin-bottom:6px">入口统一 —— Agent 调用的都是 <code>Bash</code> 工具。<strong>运行时自动识别</strong>命令特性，落到对的执行形态：</p>
        <ul style="margin:0;padding-left:18px;line-height:1.8">
          <li><strong>Bash · 同步</strong> · 短命令阻塞返回</li>
          <li><strong>BackgroundTask · 异步</strong> · 长命令后台运行</li>
          <li><strong>Session · 交互</strong> · 需要 stdin 的命令走 PTY</li>
        </ul>
      </div>
      <div class="col-visual">
        <div class="fig" data-fig="图 · 执行三态：横轴时间，纵向三泳道，虚线箭头为自动流转">
          <h4 style="font-size:14px;font-weight:600;color:var(--text-1);margin:0 0 8px;letter-spacing:.02em">运行时自动流转</h4>
          <svg viewBox="0 0 420 270" style="width:100%;height:auto;font-family:var(--font-mono);font-size:10px">
            <defs>
              <marker id="ah-e1" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
                <path d="M0,0 L10,5 L0,10 z" fill="#1e3a8a"/>
              </marker>
              <marker id="ah-e2" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
                <path d="M0,0 L10,5 L0,10 z" fill="#7a5b00"/>
              </marker>
            </defs>

            <text x="200" y="14" text-anchor="middle" fill="#6b7a8f" font-size="9">time →</text>

            <!-- Lane 1: Bash sync -->
            <text x="6" y="48" font-weight="600" fill="#0d1b2a" font-size="11">Bash · sync</text>
            <rect x="98" y="34" width="56" height="22" fill="#fff" stroke="#1e3a8a" stroke-width="1.2"/>
            <text x="126" y="49" text-anchor="middle">spawn</text>
            <line x1="154" y1="45" x2="170" y2="45" stroke="#1e3a8a" stroke-width="1" marker-end="url(#ah-e1)"/>
            <rect x="170" y="34" width="108" height="22" fill="#fff" stroke="#1e3a8a" stroke-width="1.2"/>
            <text x="224" y="49" text-anchor="middle">poll · ≤ 120s</text>
            <line x1="278" y1="45" x2="296" y2="45" stroke="#1e3a8a" stroke-width="1" marker-end="url(#ah-e1)"/>
            <rect x="296" y="34" width="80" height="22" fill="#f3faf5" stroke="#1e5e3a" stroke-width="1.2"/>
            <text x="336" y="49" text-anchor="middle" fill="#1e5e3a">stdout/err</text>
            <text x="200" y="75" text-anchor="middle" fill="#6b7a8f" font-size="9">快命令：阻塞等完返回</text>

            <!-- Auto-promote -->
            <path d="M224,56 Q224,96 224,130" stroke="#7a5b00" stroke-width="1.4" stroke-dasharray="3,2" fill="none" marker-end="url(#ah-e2)"/>
            <text x="248" y="87" fill="#7a5b00" font-size="9">&gt; 30s</text>
            <text x="248" y="99" fill="#7a5b00" font-size="9">auto → bg</text>

            <!-- Lane 2: Background -->
            <text x="6" y="144" font-weight="600" fill="#0d1b2a" font-size="11">Background</text>
            <rect x="98" y="130" width="76" height="22" fill="#fff" stroke="#1e3a8a" stroke-width="1.2"/>
            <text x="136" y="145" text-anchor="middle">spawn → tid</text>
            <line x1="174" y1="141" x2="216" y2="141" stroke="#1e3a8a" stroke-width="1" stroke-dasharray="2,2"/>
            <rect x="216" y="130" width="62" height="22" fill="#fff" stroke="#1e3a8a" stroke-width="1.2"/>
            <text x="247" y="145" text-anchor="middle">running</text>
            <line x1="278" y1="141" x2="296" y2="141" stroke="#1e3a8a" stroke-width="1" marker-end="url(#ah-e1)"/>
            <rect x="296" y="130" width="80" height="22" fill="#f3faf5" stroke="#1e5e3a" stroke-width="1.2"/>
            <text x="336" y="145" text-anchor="middle" fill="#1e5e3a">TaskOutput</text>
            <text x="247" y="168" text-anchor="middle" fill="#6b7a8f" font-size="9">reader 线程实时写 buffer</text>

            <!-- Lane 3: Session PTY -->
            <text x="6" y="196" font-weight="600" fill="#0d1b2a" font-size="11">Session · PTY</text>
            <rect x="98" y="182" width="76" height="22" fill="#fff" stroke="#1e3a8a" stroke-width="1.2"/>
            <text x="136" y="197" text-anchor="middle">PTY → sid</text>
            <line x1="174" y1="193" x2="190" y2="193" stroke="#1e3a8a" stroke-width="1" marker-end="url(#ah-e1)"/>
            <rect x="190" y="182" width="120" height="22" fill="#fdfcf8" stroke="#7a5b00" stroke-width="1.2"/>
            <text x="250" y="197" text-anchor="middle" fill="#7a5b00">stdin ⇄ stdout</text>
            <line x1="310" y1="193" x2="324" y2="193" stroke="#1e3a8a" stroke-width="1" marker-end="url(#ah-e1)"/>
            <rect x="324" y="182" width="42" height="22" fill="#fdf6f6" stroke="#7a1f1f" stroke-width="1.2"/>
            <text x="345" y="197" text-anchor="middle" fill="#7a1f1f">quit</text>
            <text x="250" y="220" text-anchor="middle" fill="#6b7a8f" font-size="9">portable_pty · drop writer → SIGHUP</text>

            <line x1="10" y1="234" x2="410" y2="234" stroke="#e5e9f2" stroke-width="0.8"/>
            <text x="10" y="250" fill="#6b7a8f" font-size="8.5">— 实线：显式调用　- - 虚线：自动流转　DEFAULT_TIMEOUT=120s · AUTO_BG=30s</text>
          </svg>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#bash #background #session</span>
      <span class="slide-number" data-current="19" data-total="37"></span>
    </div>
    <aside class="notes">
      <p>Bash 这部分我是从一个真实卡死案例倒推出来的。当时 Agent 跑 npm create vite，这个命令是交互式的，进程在等用户按回车、Agent 在等 stdout——双方互相等死锁。所以 Bash 必须有三态执行：短命令同步、长命令自动升级到后台、交互式命令走 PTY Session。关键设计是超时不直接 kill，而是先做静默检测——如果一直没有任何输出，那大概率就是在等用户输入，给 Agent 提示加 -y；如果还在出输出，就把进程移交后台、立即返回 task_id。</p>
    </aside>
  </section>

  <!-- ============ 10b. 执行三态 · 实现 ============ -->
  <section class="slide" data-title="执行三态 · impl">
    <div class="title-bar">
      <h2>03 · 面向场景强化的工具 — 执行三态实现
    </div>
    <div class="split s55">
      <div class="col-text">
        <h4 style="margin-top:0">① 超时自动升级</h4>
        <p class="dim" style="line-height:1.6;margin:4px 0 6px">同步 Bash 跑过 <code>SHELL_AUTO_BG_SECS</code>（30s）仍未结束：</p>
        <ul style="line-height:1.7;margin:0;padding-left:18px">
          <li>静默检测 — N 秒无输出 → 疑似交互 → kill + 提示</li>
          <li>非静默 → <span class="accent">不 kill</span>，<code>adopt_process</code> 移交 BackgroundManager</li>
          <li>立即返回 <code>task_id</code>，Agent 继续做别的事</li>
          <li>后台监控线程等 reader 写完后调 <code>complete_task</code></li>
        </ul>
        <h4 style="margin-top:14px">② PTY 交互式会话</h4>
        <p class="dim" style="line-height:1.6;margin:4px 0 6px">用 <code>portable_pty</code> 分配伪终端：</p>
        <ul style="line-height:1.7;margin:0;padding-left:18px">
          <li><code>openpty</code> → <code>spawn_command</code> 启动进程</li>
          <li><code>try_clone_reader</code> → Session stdout</li>
          <li><code>take_writer</code> → <code>set_pty_writer</code> 注册到 BgTask</li>
          <li>Session 工具的 <code>stdin/stdout/quit</code> 直接操作 PTY 句柄</li>
          <li>quit = drop writer → SIGHUP 自然退出</li>
        </ul>
      </div>
      <div class="col-visual">
        <div class="code-card" style="margin-top:0">
          <div class="cc-head">
            <span><span class="cc-path">shell.rs</span> · execute_sync 自动升级</span>
            <span class="cc-tag">real code</span>
          </div>
<pre style="font-size:11px;line-height:1.5;padding:8px 10px"><span class="cm">// 超过阈值仍未结束，不 kill，移交后台</span>
<span class="kw">if</span> start.elapsed() &gt;= auto_bg_threshold {
    <span class="cm">// 静默判交互：最近 N 秒无输出 → 等 stdin</span>
    <span class="kw">if</span> silence_elapsed &gt;= Duration::<span class="fn">from_secs</span>(SILENCE) {
        <span class="kw">let</span> _ = child.<span class="fn">kill</span>();  <span class="cm">// 提示加 --yes</span>
        <span class="kw">return</span> ToolResult { output: hint, is_error: <span class="kw">true</span>, .. };
    }
    <span class="cm">// 真正升级：adopt 当前进程，立即返回 task_id</span>
    <span class="kw">let</span> (task_id, bg_buffer) =
        <span class="hl">self.manager.<span class="fn">adopt_process</span>(command, pid, start)</span>;
    <span class="cm">// 后台监控：等 reader 写完 → complete_task</span>
    std::thread::<span class="fn">spawn</span>(<span class="kw">move</span> || {
        <span class="kw">let</span> _ = stdout_thread.<span class="fn">join</span>();
        manager.<span class="fn">complete_task</span>(&amp;tid, <span class="str">"completed"</span>, out);
    });
    <span class="kw">return</span> ToolResult { output: json!({ <span class="str">"task_id"</span>: task_id }), .. };
}</pre>
        </div>

        <div class="code-card">
          <div class="cc-head">
            <span><span class="cc-path">shell.rs</span> · execute_interactive 起 PTY</span>
            <span class="cc-tag">real code</span>
          </div>
<pre style="font-size:11px;line-height:1.5;padding:8px 10px"><span class="kw">let</span> pty = <span class="fn">NativePtySystem::default</span>();
<span class="kw">let</span> pair = pty.<span class="fn">openpty</span>(PtySize { rows: <span class="num">24</span>, cols: <span class="num">80</span>, .. })?;
<span class="kw">let</span> _child = pair.slave.<span class="fn">spawn_command</span>(builder)?;
<span class="kw">let</span> reader = pair.master.<span class="fn">try_clone_reader</span>()?;   <span class="cm">// stdout</span>
<span class="kw">let</span> writer = pair.master.<span class="fn">take_writer</span>()?;         <span class="cm">// stdin</span>
<span class="hl">self.manager.<span class="fn">set_pty_writer</span>(&amp;task_id, writer)</span>;
<span class="cm">// Session 工具通过 sid 调 stdin / stdout / quit</span></pre>
        </div>
      </div>
    </div>
    <div class="deck-footer">
      <span class="mono">#adopt_process #portable_pty</span>
      <span class="slide-number" data-current="20" data-total="37"></span>
    </div>
    <aside class="notes">
      <p>（这页是上一页两个机制的真实代码，扫一眼即可，不停顿。）</p>
    </aside>
  </section>



  <!-- ============ §04 分隔页 ============ -->
  <section class="slide chapter-divider" data-title="§04 Divider">
    <span class="ch-num">Section 04</span>
    <div class="ch-title">多 Agent 协作</div>
    <p class="ch-sub">两种协作模式——Teammate 对等协作 + SubAgent 委派调研。共享消息通道的设计、可见性控制、以及三个递进实验验证。</p>
  </section>

  <!-- ============ 11. MULTI-AGENT COLLABORATION ============ -->
  <section class="slide" data-title="Multi-Agent Collaboration">
    <div class="title-bar">
      <h2>04 · 多 Agent 协作 — 两种协作模式
    </div>
    <div class="split s55">
      <div class="col-text">
        <h4 style="margin-top:0">Teammate · 对等协作</h4>
        <p>独立 Agent 实例，各有专属上下文和工具集。通过 <code>SendMessage</code> 广播 + <code>@提及</code> 定向通信。支持 <code>WorkDone</code> 声明完成，<code>@self</code> 可重新激活。多 Teammate 共享消息通道，形成聊天室。</p>
        <h4>SubAgent · 委派调研</h4>
        <p>临时子 Agent，<strong>独立窗口完全隔离</strong>。无收件箱、不能发消息。执行完毕后仅回灌精简摘要（~1.5K tokens）到主 Agent 上下文，主窗口仅微增 4%。</p>
        <h4>客观评估</h4>
        <p class="dim">参考 Anthropic 研究——模型倾向偏袒自己的工作。通过聊天室让其他 Agent 交叉评审，实现相对客观的质量评估。</p>
      </div>
      <div class="col-visual">
        <div class="ma-arch">
          <div class="ma-section">
            <h5>Teammate 协作架构</h5>
            <div class="ma-row">
              <div class="ma-node tm-node"><b>Frontend</b><p>独立上下文<br>独立工具集</p></div>
              <div class="ma-arr">⇄</div>
              <div class="ma-node main-node"><b>Main Agent</b><p>协调调度<br>共享消息通道</p></div>
              <div class="ma-arr">⇄</div>
              <div class="ma-node tm-node"><b>Backend</b><p>独立上下文<br>独立工具集</p></div>
            </div>
            <div class="ma-channels">
              <span class="ma-ch ch-bi">broadcast_inbox</span>
              <span class="ma-ch ch-ctx">context_messages</span>
              <span class="ma-ch ch-disp">display_messages</span>
            </div>
          </div>
          <div class="ma-section">
            <h5>SubAgent 委派架构</h5>
            <div class="ma-row">
              <div class="ma-node main-node"><b>Main Agent</b><p>派遣任务<br>接收摘要</p></div>
              <div class="ma-arr">→<br><span style="font-size:9px;color:var(--text-3)">prompt</span></div>
              <div class="ma-node sub-node"><b>SubAgent</b><p>独立窗口<br>独立工具集</p></div>
              <div class="ma-arr">←<br><span style="font-size:9px;color:var(--text-3)">摘要</span></div>
              <div class="ma-node" style="border-top:2px solid var(--good)"><b>context</b><p>XML-wrapped<br>&lt;SubAgent&gt;摘要&lt;/SubAgent&gt;</p></div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>多 Agent 我做了两种模式。Teammate 是对等协作者，每个有独立窗口和工具集，通过共享聊天室广播沟通。SubAgent 是临时调研员，独立窗口完全隔离，执行完只把一段精简摘要回灌给主 Agent——这样三个并行调研合计 70K token，主 Agent 只多用了 4%。</p>
    </aside>
  </section>

  <!-- ============ 11.5. MESSAGE DESIGN + VISIBILITY ============ -->
  <section class="slide" data-title="Message Design & Visibility">
    <div class="title-bar">
      <h2>04 · 多 Agent 协作 — 消息通信设计与可见性
    </div>
    <div class="split s55">
      <div class="col-text">
        <h4 style="margin-top:0">三通道架构</h4>
        <ul>
          <li><code>broadcast_inbox</code> — 每个 Teammate 的收件箱，其他 Agent 广播投递</li>
          <li><code>context_messages</code> — 注入 Main Agent LLM 上下文（XML-wrapped <code>&lt;From&gt;text&lt;/From&gt;</code>）</li>
          <li><code>display_messages</code> — TUI 渲染（clean text + sender_name）</li>
        </ul>
        <h4>设计动机</h4>
        <p>Agent Loop 长时序异步，Agent 发完消息不原地等确认 → 下一轮可能因过期快照重发。这一问题的实验验证与解决方案（SendMessage Gate）在后页详述。</p>
      </div>
      <div class="col-visual">
        <h4 style="margin-top:0;font-size:14px;color:var(--text-3)">消息可见性矩阵</h4>
        <table class="vis-table">
          <tr><th></th><th>Main Agent</th><th>Teammate</th><th>SubAgent</th></tr>
          <tr><td>Own messages</td><td class="yes">Full</td><td class="yes">Full</td><td class="yes">Full</td></tr>
          <tr><td>Other agent text</td><td class="yes">context (XML)</td><td class="yes">broadcast_inbox</td><td class="no">None</td></tr>
          <tr><td>Other tool calls</td><td class="yes">&lt;Name&gt;[called X]&lt;/Name&gt;</td><td class="no">Not visible</td><td class="no">None</td></tr>
          <tr><td>Other tool results</td><td class="no">Not pushed</td><td class="no">Not visible</td><td class="no">None</td></tr>
          <tr><td>Can SendMessage</td><td class="yes">Yes</td><td class="yes">Yes</td><td class="no">No</td></tr>
          <tr><td>Can receive</td><td class="yes">main_inbox</td><td class="yes">broadcast_inbox</td><td class="no">No</td></tr>
        </table>
        <p class="dim" style="font-size:14px;margin-top:10px;line-height:1.6"><strong>设计原则</strong>: 决策信息多放（other agent text），执行中间过程少放（tool results 不推）。SubAgent 完全隔离——不能收发消息，只回灌摘要。</p>
      </div>
    </div>
    <aside class="notes">
      <p>为了支撑这两种模式，消息层我做了三通道架构和信息不对称的可见性：Main 看得到队友的文本和工具调用名，Teammate 之间只看广播文本，SubAgent 完全隔离——决策信息多放、执行细节少放。</p>
    </aside>
  </section>

  <!-- ============ Exp1a — 轮流报数：问题 ============ -->
  <section class="slide" data-title="Exp1a · 轮流报数 — 问题">
    <div class="title-bar">
      <h2>04 · 多 Agent 协作试验 — 轮流报数：过期快照问题
    </div>
    <div class="split s55">
      <div class="col-text" style="justify-content:center">
        <h4 style="margin-top:0">实验内容</h4>
        <p>两个 Teammate 轮流报递增数字（1, 2, 3 …），验证多 Agent 之间的<strong>消息传递与唤醒</strong>能力。仅通过 SendMessage 沟通，不共享全局状态。</p>
        <h4 style="color:var(--bad)">发现的问题 · 过期快照导致重复消息</h4>
        <p>1 号 Agent 进入循环时，2 号已报的"2"尚未到达其信箱。1 号快照只有 [1]，于是催促 2 号报 2 → 2 号看到 [1,2] + 催促 → 重复报 2 → <strong>对外不一致</strong>。</p>
        <p class="dim" style="margin-top:8px">根本原因：Agent Loop 长时序异步，发完 SendMessage 不原地等确认 → 下一轮基于过期快照决策。</p>
      </div>
      <div class="col-visual">
        <svg viewBox="0 0 400 170" style="width:100%;height:auto;font-family:var(--font-mono);font-size:10px">
          <defs>
            <marker id="ah-prob" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
              <path d="M0,0 L10,5 L0,10 z" fill="#6b7a8f"/>
            </marker>
          </defs>
          <!-- 泳道标题 -->
          <rect x="10" y="0" width="70" height="22" fill="#1e3a8a" rx="3"/>
          <text x="45" y="15" text-anchor="middle" fill="#fff" font-weight="600" font-size="11">1 号 Agent</text>
          <rect x="320" y="0" width="70" height="22" fill="#7a5b00" rx="3"/>
          <text x="355" y="15" text-anchor="middle" fill="#fff" font-weight="600" font-size="11">2 号 Agent</text>
          <!-- 泳道竖线 -->
          <line x1="45" y1="26" x2="45" y2="155" stroke="#1e3a8a" stroke-width="1" stroke-dasharray="4,3"/>
          <line x1="355" y1="26" x2="355" y2="155" stroke="#7a5b00" stroke-width="1" stroke-dasharray="4,3"/>
          <!-- 1号报1 -->
          <rect x="20" y="32" width="50" height="18" fill="#f5f8ff" stroke="#1e3a8a" stroke-width="1" rx="2"/>
          <text x="45" y="44" text-anchor="middle" fill="#1e3a8a" font-weight="600">报 1</text>
          <line x1="70" y1="41" x2="310" y2="56" stroke="#1e3a8a" stroke-width="1" marker-end="url(#ah-prob)"/>
          <text x="190" y="44" text-anchor="middle" fill="#6b7a8f" font-size="9">SendMessage "1"</text>
          <!-- 2号报2 -->
          <rect x="330" y="52" width="50" height="18" fill="#fefdf5" stroke="#7a5b00" stroke-width="1" rx="2"/>
          <text x="355" y="64" text-anchor="middle" fill="#7a5b00" font-weight="600">报 2</text>
          <line x1="330" y1="66" x2="90" y2="83" stroke="#7a5b00" stroke-width="1" stroke-dasharray="3,3"/>
          <text x="210" y="72" text-anchor="middle" fill="#7a1f1f" font-size="9">❌ 尚未到达 1 号信箱</text>
          <!-- 1号催促2号 -->
          <rect x="10" y="80" width="70" height="18" fill="#fff5f5" stroke="#7a1f1f" stroke-width="1" rx="2"/>
          <text x="45" y="92" text-anchor="middle" fill="#7a1f1f" font-weight="600">催 2 号报 2</text>
          <line x1="80" y1="89" x2="310" y2="98" stroke="#7a1f1f" stroke-width="1" marker-end="url(#ah-prob)"/>
          <text x="195" y="94" text-anchor="middle" fill="#6b7a8f" font-size="9">"请报 2"（快照只有 [1]）</text>
          <!-- 2号重复报2 -->
          <rect x="320" y="96" width="70" height="18" fill="#fff5f5" stroke="#7a1f1f" stroke-width="1.5" rx="2"/>
          <text x="355" y="108" text-anchor="middle" fill="#7a1f1f" font-weight="700">重复报 2!</text>
          <!-- 不一致标注 -->
          <text x="200" y="135" text-anchor="middle" fill="#7a1f1f" font-size="10" font-weight="600">对外不一致：2 被报了两次</text>
          <text x="200" y="160" text-anchor="middle" fill="var(--text-3)" font-size="9">Agent Loop 长时序 → 过期快照 → 重复决策</text>
        </svg>
      </div>
    </div>
    <aside class="notes">
      <p>第一个实验是两个 Teammate 轮流报数。本来只是想验证消息传递，但实验里反而暴露了一个我没预想到的问题——过期快照导致重复消息：Agent 发完消息不等确认就进入下一轮，看到的还是旧信箱。</p>
    </aside>
  </section>

  <!-- ============ Exp1b — SendMessage Gate 解决 ============ -->
  <section class="slide" data-title="Exp1b · SendMessage Gate">
    <div class="title-bar">
      <h2>04 · 多 Agent 协作试验 — 二阶段提交 Gate 与实测结果
    </div>
    <div class="split s55">
      <div class="col-text">
        <h4 style="margin-top:0;color:var(--good);margin-bottom:4px">解决 · SendMessage Gate（二阶段提交）</h4>
        <p style="margin-bottom:6px">Agent 调 SendMessage 前，先检查 <code>broadcast_inbox</code> 有无未读消息。有 → hold + 注入 <code>system_reminder</code> → 信箱串行化 → Agent 基于最新状态<strong>二次确认</strong>（commit 或重新决策），最多重试 2 次。</p>
        <div class="code-card" style="margin-top:0;margin-bottom:8px">
          <div class="cc-head">
            <span><span class="cc-path">teammate_loop.rs</span> · SendMessage Gate</span>
          </div>
<pre style="font-size:10px;line-height:1.35;padding:4px 8px"><span class="kw">if</span> item.name == <span class="str">"SendMessage"</span> {
    <span class="kw">let</span> inbox_has_new = inbox.lock()
        .map(|i| !i.is_empty()).unwrap_or(<span class="kw">false</span>);
    <span class="kw">if</span> inbox_has_new && retries &lt; MAX_GATE_RETRIES {
        gate_triggered = <span class="kw">true</span>;
        <span class="cm">// hold: return placeholder, inject system_reminder</span>
        <span class="kw">continue</span>;
    }
}</pre>
        </div>
        <h4 style="margin-bottom:2px">实测结果</h4>
        <table class="exp-table" style="margin-top:0;font-size:12px">
          <tr><td>目标数字数</td><td class="num">10</td></tr>
          <tr class="ok"><td>正确报数数</td><td class="num">10 / 10 · <strong>100%</strong></td></tr>
          <tr class="ok"><td>顺序错误数</td><td class="num">0</td></tr>
          <tr class="ok"><td>唤醒失败数</td><td class="num">0</td></tr>
          <tr><td>SendMessage 调用</td><td class="num">16 次</td></tr>
          <tr class="ok"><td>人工干预次数</td><td class="num">0</td></tr>
        </table>
      </div>
      <div class="col-visual">
        <svg viewBox="0 0 400 180" style="width:100%;height:auto;font-family:var(--font-mono);font-size:10px">
          <defs>
            <marker id="ah-sol" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
              <path d="M0,0 L10,5 L0,10 z" fill="#6b7a8f"/>
            </marker>
          </defs>
          <!-- 泳道标题 -->
          <rect x="10" y="0" width="70" height="22" fill="#1e3a8a" rx="3"/>
          <text x="45" y="15" text-anchor="middle" fill="#fff" font-weight="600" font-size="11">1 号 Agent</text>
          <rect x="320" y="0" width="70" height="22" fill="#7a5b00" rx="3"/>
          <text x="355" y="15" text-anchor="middle" fill="#fff" font-weight="600" font-size="11">2 号 Agent</text>
          <!-- 泳道竖线 -->
          <line x1="45" y1="26" x2="45" y2="170" stroke="#1e3a8a" stroke-width="1" stroke-dasharray="4,3"/>
          <line x1="355" y1="26" x2="355" y2="170" stroke="#7a5b00" stroke-width="1" stroke-dasharray="4,3"/>
          <!-- 1号报1 -->
          <rect x="20" y="32" width="50" height="18" fill="#f5f8ff" stroke="#1e3a8a" stroke-width="1" rx="2"/>
          <text x="45" y="44" text-anchor="middle" fill="#1e3a8a" font-weight="600">报 1</text>
          <line x1="70" y1="41" x2="310" y2="52" stroke="#1e3a8a" stroke-width="1" marker-end="url(#ah-sol)"/>
          <text x="190" y="44" text-anchor="middle" fill="#6b7a8f" font-size="9">SendMessage "1"</text>
          <!-- 2号报2 -->
          <rect x="330" y="48" width="50" height="18" fill="#fefdf5" stroke="#7a5b00" stroke-width="1" rx="2"/>
          <text x="355" y="60" text-anchor="middle" fill="#7a5b00" font-weight="600">报 2</text>
          <line x1="330" y1="62" x2="90" y2="80" stroke="#7a5b00" stroke-width="1"/>
          <text x="210" y="70" text-anchor="middle" fill="#6b7a8f" font-size="9">SendMessage "2"</text>
          <!-- 1号欲报3，Gate拦截 -->
          <rect x="10" y="80" width="70" height="18" fill="#f0fff4" stroke="#1e5e3a" stroke-width="1" rx="2"/>
          <text x="45" y="92" text-anchor="middle" fill="#1e5e3a" font-weight="600">欲报 3</text>
          <rect x="90" y="82" width="90" height="16" fill="#1e5e3a" rx="3"/>
          <text x="135" y="93" text-anchor="middle" fill="#fff" font-size="9" font-weight="600">Gate: 信箱有新消息</text>
          <!-- 串行化确认 -->
          <rect x="20" y="108" width="70" height="18" fill="#1e5e3a" rx="3"/>
          <text x="55" y="120" text-anchor="middle" fill="#fff" font-weight="600" font-size="10">二次确认</text>
          <text x="200" y="120" text-anchor="middle" fill="#1e5e3a" font-size="9">信箱串行化 · 看到 [1,2] → 确认报 3</text>
          <!-- 1号报3 -->
          <rect x="20" y="136" width="50" height="18" fill="#f5f8ff" stroke="#1e3a8a" stroke-width="1" rx="2"/>
          <text x="45" y="148" text-anchor="middle" fill="#1e3a8a" font-weight="600">报 3 ✓</text>
          <text x="200" y="148" text-anchor="middle" fill="#1e5e3a" font-size="10" font-weight="600">对外一致：1, 2, 3 严格递增</text>
        </svg>
      </div>
    </div>
    <aside class="notes">
      <p>我的解决办法是 SendMessage Gate，二阶段提交：发消息之前先检查信箱有没有新消息，有就 hold 住、注入提醒、让 Agent 看到最新状态后再决策。修复后 10 个数字全部正确报出、人工干预 0 次。</p>
    </aside>
  </section>

  <!-- ============ 11.7b. EXPERIMENT 02 — 续写接龙 ============ -->
  <section class="slide" data-title="Exp2 · 续写接龙">
    <div class="title-bar">
      <h2>04 · 多 Agent 协作试验 — 共享上下文一致性
    </div>
    <div class="split s55">
      <div class="col-text" style="justify-content:center">
        <h4 style="margin-top:0">任务设置</h4>
        <p>多个 Teammate 轮流续写一句故事，保持人物、地点、时间线和事件因果一致。</p>
        <h4>检验目标</h4>
        <p>实验一只验证"能按序发言"，本实验进一步检验<strong>共享上下文</strong>——每个 Agent 是否真能读到前文设定。如果 shared_messages 广播有 bug，故事会立即跑题。</p>
        <h4>验证方式</h4>
        <p>统计完成轮数、偏题次数、重复内容次数、人工干预次数。</p>
      </div>
      <div class="col-visual">
        <div class="exp-result">
          <div class="er-head">论文表 7-11 · 实测结果</div>
          <table class="exp-table">
            <tr><td>目标续写轮数</td><td class="num">13</td></tr>
            <tr class="ok"><td>完成续写轮数</td><td class="num">13 / 13 · <strong>100%</strong></td></tr>
            <tr class="ok"><td>偏题次数</td><td class="num">0</td></tr>
            <tr class="warn"><td>重复内容次数</td><td class="num">1</td></tr>
            <tr class="ok"><td>人工干预次数</td><td class="num">0</td></tr>
          </table>
          <p class="er-note"><strong>共享上下文有效</strong>：13 轮全部完成，偏题 0、人工干预 0。仅 1 次重复——角色「反转之王」被催促后重发了同句，提示仍需<strong>消息去重 / 过期约束</strong>。</p>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>实验二把验证从"能按序发言"推进到"能否沿用同一组事实"——多个 Teammate 轮流续写故事、保持人物时间线一致。13 轮全部完成、偏题 0 次；只有 1 次内容重复，说明协作协议本身没问题，但仍需要消息去重。</p>
    </aside>
  </section>

  <!-- ============ 11.7c. EXPERIMENT 03 — 开发-测试协作 ============ -->
  <section class="slide" data-title="Exp3 · 协作开发">
    <div class="title-bar">
      <h2>04 · 多 Agent 协作试验 — 开发-测试协作
    </div>
    <div class="split s55">
      <div class="col-text" style="justify-content:center">
        <h4 style="margin-top:0">What · 任务设置</h4>
        <p>Developer + Tester 双角色协作。任务是给前面端到端生成的电商网页项目<strong>追加 45 项验收清单</strong>（A 类页面跳转 20 + B 类浏览历史 15 + C 类 UX 改进 10）。Tester 把需求拆为可验收项 → 跑构建/静态检查 → SendMessage 反馈失败项 → Developer 修复 → 复验。</p>
        <h4>Why · 客观评估的关键</h4>
        <p>参考 Anthropic <em>harness-design-long-running-apps</em>：<strong>模型对自己的工作有偏袒</strong>，倾向给自己打高分。让<strong>另一个 Agent 做评审</strong>才能得到相对客观的反馈，形成"实现—验证—反馈—修复—复验"的工程闭环。</p>
      </div>
      <div class="col-visual">
        <div class="exp-result">
          <div class="er-head">论文表 7-13 · 实测结果</div>
          <table class="exp-table">
            <tr class="ok"><td>A 类 · 页面跳转</td><td class="num">20 / 20</td></tr>
            <tr class="ok"><td>B 类 · 浏览历史</td><td class="num">15 / 15</td></tr>
            <tr class="ok"><td>C 类 · UX 改进</td><td class="num">10 / 10</td></tr>
            <tr class="ok"><td><strong>需求完成率</strong></td><td class="num"><strong>45 / 45 · 100%</strong></td></tr>
            <tr><td>第 1 轮验收</td><td class="num">41 通过 / 4 失败</td></tr>
            <tr class="ok"><td>第 2 轮复验</td><td class="num">45 / 45 全过</td></tr>
            <tr><td>Tester 反馈轮次</td><td class="num">2 轮</td></tr>
            <tr><td>Developer 修复轮次</td><td class="num">1 轮</td></tr>
            <tr class="ok"><td>协同冲突 / 重复劳动</td><td class="num">0 / 0</td></tr>
            <tr><td>人工干预</td><td class="num">2 次（仅流程调度）</td></tr>
          </table>
          <p class="er-note">闭环完整：<strong>需求清单 → 实现 → 首轮验收 → 反馈 → 集中修复 → 复验通过</strong>。Tester 发现的 4 个失败项都是局部细节（字段缺失、路径不一致等），未涉及架构级偏差。</p>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>实验三是真实工程场景：Developer + Tester 协作完成 45 项验收清单。这里有个我比较看重的参考——Anthropic 那篇论文指出模型对自己的工作有偏袒，让另一个 Agent 来评审才能拿到客观反馈。结果是第一轮 41 通过、4 项失败，Developer 一次性修完、第二轮 45/45 全过；需求完成率 100%、协同冲突 0。</p>
    </aside>
  </section>

  <!-- ============ 11.8. SUBAGENT DELEGATION ============ -->
  <section class="slide" data-title="SubAgent Delegation">
    <div class="title-bar">
      <h2>04 · 多 Agent 协作 — SubAgent：低上下文成本的调研委派
    </div>
    <div class="split s55">
      <div class="col-text" style="overflow:hidden">
        <h4 style="margin-top:0;margin-bottom:2px">上下文隔离</h4>
        <p style="margin-bottom:2px">独立消息窗口，不继承父 Agent 历史。以 <code>prompt</code> 为唯一起点，在独立窗口里 Read / Grep / WebFetch。</p>
        <h4 style="margin-bottom:2px">工具隔离</h4>
        <p style="margin-bottom:2px">独立 <code>ToolRegistry</code>，排除 <code>Teammate</code> / <code>Agent</code> 工具（防递归）。可选 <code>worktree</code> 创建独立 git 工作树。</p>
        <h4 style="margin-bottom:2px">结果回灌</h4>
        <p style="margin-bottom:2px">执行完毕后，仅回灌精简摘要到主 Agent：</p>
        <ul style="line-height:1.5;margin:0;padding-left:18px">
          <li><code>display</code> — clean text + sender_name</li>
          <li><code>context</code> — <code>&lt;SubAgent@Name&gt;摘要&lt;/SubAgent@Name&gt;</code></li>
          <li>工具结果<strong>不推入</strong> context（token 高、价值低）</li>
        </ul>
      </div>
      <div class="col-visual">
        <div class="subtree">
          <div class="root">
            <b>主 Agent</b>
            <span>context: 38%</span>
          </div>
          <div class="conn-bidir">
            <span class="dir-down">↓ 派遣调研任务</span>
            <span class="dir-up">↑ 仅回灌精简摘要 (~1.5K tokens)</span>
          </div>
          <div class="branches">
            <div class="branch">
              <span class="tag">SubAgent A</span>
              <b>调研 auth 模块</b>
              <p>Read 12 文件<br>Grep 8 次</p>
              <p class="dim" style="margin-top:6px">独立窗口 ~22K tokens</p>
            </div>
            <div class="branch">
              <span class="tag">SubAgent B</span>
              <b>调研 storage 层</b>
              <p>Read 18 文件<br>Glob 项目结构</p>
              <p class="dim" style="margin-top:6px">独立窗口 ~31K tokens</p>
            </div>
            <div class="branch">
              <span class="tag">SubAgent C</span>
              <b>调研 hook 系统</b>
              <p>WebFetch 文档<br>读 README</p>
              <p class="dim" style="margin-top:6px">独立窗口 ~18K tokens</p>
            </div>
          </div>
          <div class="summary">
            三份调研合计 ~71K tokens 在 SubAgent 独立窗口里消耗，主 Agent 仅微增 <strong>4%</strong>
          </div>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>SubAgent 的价值在于用很低的上下文成本去理解未知模块。它在窗口、工具、结果回灌三个层面都跟主 Agent 隔离，主 Agent 只拿到精简结论，不被原始噪音污染。</p>
    </aside>
  </section>


  <!-- ============ §05 分隔页 ============ -->
  <section class="slide chapter-divider" data-title="§05 Divider">
    <span class="ch-num">Section 05</span>
    <div class="ch-title">模板代码生成引擎</div>
    <p class="ch-sub">把高度模板化的代码交给确定性生成器，LLM 只做需求理解和增量集成。jen 处理数据层，webapp-gen 驱动全栈工作流。</p>
  </section>

  <!-- ============ §5.1 问题 ============ -->
  <section class="slide" data-title="Template Engine · problem">
    <div class="title-bar">
      <h2>05 · 模板代码生成引擎 — 问题
    </div>
    <div class="split s55">
      <div class="col-text" style="justify-content:center">
        <h4 style="margin-top:0;color:var(--bad)">让 LLM 写模板代码的代价</h4>
        <p>业务系统中存在大量<strong>高度模板化</strong>的代码——PO / DTO / VO / DAO、CRUD 方法、列表查询。它们<strong>占比高</strong>，但<strong>业务含量有限</strong>。让 LLM 逐字生成，会怎样？</p>
        <h4 style="margin-top:16px">51 张表 DAO 生成实测（论文 §7.4）</h4>
        <ul style="line-height:1.8">
          <li><strong>11 个字段遗漏</strong> · 247 / 258，字段一致性仅 95.7%</li>
          <li><strong>4 个索引方法缺失</strong> · 22 / 26，写到第 30 张表时已忘了第 5 张的唯一索引</li>
          <li><strong>24M token · 53 min</strong> · LLM 调用 582 次，工具调用 2,006 次</li>
          <li><strong>67 次上下文压缩</strong> · 长链路推理撑爆上下文窗口</li>
        </ul>
        <h4 style="margin-top:16px">本质</h4>
        <p>概率性的<strong>长链路推理的系统性风险</strong>——模板代码字段多、重复度高，累积误差不可避免。</p>
      </div>
      <div class="col-visual">
        <div class="fig" data-fig="图 · 模板化生成 vs LLM 现场生成">
          <svg viewBox="0 0 380 280" style="width:100%;height:auto;font-family:var(--font-mono);font-size:11px">
            <defs>
              <marker id="ah-tg" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
                <path d="M0,0 L10,5 L0,10 z" fill="#1e3a8a"/>
              </marker>
            </defs>
            <text x="190" y="18" text-anchor="middle" fill="#6b7a8f">两条路径处理同一份 SQL schema</text>

            <!-- LLM 直接生成 -->
            <text x="20" y="50" font-weight="600" fill="#7a1f1f">LLM 直接生成</text>
            <rect x="20" y="60" width="80" height="32" fill="#fdf6f6" stroke="#7a1f1f" stroke-width="1.2"/>
            <text x="60" y="80" text-anchor="middle">SQL</text>
            <line x1="100" y1="76" x2="118" y2="76" stroke="#7a1f1f" stroke-width="1.2" marker-end="url(#ah-tg)"/>
            <rect x="118" y="60" width="120" height="32" fill="#fdf6f6" stroke="#7a1f1f" stroke-width="1.2"/>
            <text x="178" y="80" text-anchor="middle">LLM 逐字推理</text>
            <line x1="238" y1="76" x2="256" y2="76" stroke="#7a1f1f" stroke-width="1.2" marker-end="url(#ah-tg)"/>
            <rect x="256" y="60" width="100" height="32" fill="#fdf6f6" stroke="#7a1f1f" stroke-width="1.2"/>
            <text x="306" y="80" text-anchor="middle">PO/DTO/VO/DAO</text>
            <text x="190" y="110" text-anchor="middle" fill="#7a1f1f" font-size="10">字段漂移 · token 高 · 慢</text>

            <!-- 分隔 -->
            <line x1="20" y1="135" x2="360" y2="135" stroke="#e5e9f2" stroke-dasharray="2,2"/>

            <!-- 模板化生成 -->
            <text x="20" y="160" font-weight="600" fill="#1e5e3a">模板化生成（jen）</text>
            <rect x="20" y="170" width="60" height="32" fill="#fbfdfb" stroke="#1e5e3a" stroke-width="1.2"/>
            <text x="50" y="190" text-anchor="middle">SQL</text>
            <line x1="80" y1="186" x2="98" y2="186" stroke="#1e5e3a" stroke-width="1.2" marker-end="url(#ah-tg)"/>
            <rect x="98" y="170" width="74" height="32" fill="#fbfdfb" stroke="#1e5e3a" stroke-width="1.2"/>
            <text x="135" y="190" text-anchor="middle">parser</text>
            <line x1="172" y1="186" x2="190" y2="186" stroke="#1e5e3a" stroke-width="1.2" marker-end="url(#ah-tg)"/>
            <rect x="190" y="170" width="80" height="32" fill="#fbfdfb" stroke="#1e5e3a" stroke-width="1.2"/>
            <text x="230" y="190" text-anchor="middle">template</text>
            <line x1="270" y1="186" x2="288" y2="186" stroke="#1e5e3a" stroke-width="1.2" marker-end="url(#ah-tg)"/>
            <rect x="288" y="170" width="68" height="32" fill="#fbfdfb" stroke="#1e5e3a" stroke-width="1.2"/>
            <text x="322" y="190" text-anchor="middle">代码</text>
            <text x="190" y="220" text-anchor="middle" fill="#1e5e3a" font-size="10">100% 字段对齐 · token 1/50 · 27× 加速</text>

            <text x="190" y="260" text-anchor="middle" fill="#6b7a8f" font-size="10">数字来源：论文表 7-8 / 7-9 · 51 张表 DAO 全生成</text>
          </svg>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>业务系统里有大量 PO/DTO/VO/DAO 这种高度模板化的代码。我做过实测：让 LLM 逐字生成 51 张表的 DAO，结果漏了 11 个字段、4 个索引方法，烧了 24M token、跑了 53 分钟、压缩触发了 67 次。这其实不是偶发错误，而是长链路推理的系统性风险。</p>
    </aside>
  </section>

  <!-- ============ §5.2 核心观察 ============ -->
  <section class="slide" data-title="Template Engine · observation">
    <div class="title-bar">
      <h2>05 · 模板代码生成引擎 — 核心观察
    </div>
    <div class="split s55">
      <div class="col-text">
        <h4 style="margin-top:0">观察 · DDL 是数据层代码的<strong>充分输入</strong></h4>
        <p>DAO 层和 Model 层的代码对<strong>业务的可操作独立发挥空间有限</strong>。只要拿到 DDL，就可以推断出：</p>
        <ul style="line-height:1.8">
          <li><strong>索引情况</strong> · 主键 → GetByPK，唯一索引 → GetByUK，普通索引 → GetByIdx</li>
          <li><strong>字段名 & 类型映射</strong> · MySQL bigint → Go int64，varchar → string，可空 → 指针</li>
          <li><strong>查询场景</strong> · 索引组合决定查询方法集，CRUD 是通用骨架</li>
          <li><strong>数据结构</strong> · PO / DTO / VO 的字段完全由 schema 列决定</li>
        </ul>
        <h4>推论</h4>
        <p>既然 DDL 是充分输入，这些代码<strong>不需要 LLM 推理</strong>——交给确定性生成器（jen），封装为 Skill 按需加载。LLM 只负责需求理解 + 准备 schema + 验收产物。</p>
        <p class="note">右侧：一份 DDL 如何推导出 Model struct + DAO 方法集。</p>
      </div>
      <div class="col-visual">
        <div class="fig-shot" style="display:flex;flex-direction:column;overflow:hidden">
          <img src="pics/jen_code.png" alt="DDL 推导 Model + DAO 对应关系" style="flex:1;min-height:0;object-fit:contain;object-position:center top;border-radius:4px;border:1px solid var(--border)">
          <div class="shot-caption">DDL → Model struct（字段名、类型映射） + DAO 方法集（按索引派生查询）</div>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>但仔细看这类代码会发现，它几乎不需要创造性决策：字段名直接映射、类型有固定对应、索引类型决定该有哪些查询方法。换句话说——DDL 已经是数据层代码的充分输入。既然如此，这部分就没必要让 LLM 来推理，交给确定性生成器更合适。</p>
    </aside>
  </section>

  <!-- ============ §5.3 jen 架构 ============ -->
  <section class="slide" data-title="jen Architecture">
    <div class="title-bar">
      <h2>05 · 模板代码生成引擎 — jen 原理
    </div>
    <div class="jen-top-bottom">
      <div class="jtb-row">
        <div class="jtb-col">
          <span class="jtb-label">① 输入</span>
          <p>建表 SQL 或直连数据库。提取表名、列定义、索引定义作为<strong>唯一事实源</strong>。</p>
        </div>
        <div class="jtb-col">
          <span class="jtb-label">② 解析</span>
          <p>自研 SQL parser 提取<strong>索引语义</strong>——主键 / 唯一索引 / 普通索引——并做 MySQL→Go 类型映射和可空字段指针推导。</p>
        </div>
        <div class="jtb-col">
          <span class="jtb-label">③ 渲染</span>
          <p>结构化 Schema 拼入 Go template。5 个 generator 分别产出 PO/DTO/VO/DAO/Tool，支持 itea-go 和 gorm 两套框架。</p>
        </div>
        <div class="jtb-col">
          <span class="jtb-label">④ 产物</span>
          <p>按索引派生查询方法：主键→<code>GetByPK</code>，唯一索引→<code>GetByUK</code>，普通索引→<code>GetByIdx</code>。确定性输出，零漂移。</p>
        </div>
      </div>
      <div class="jtb-diagram">
        <div class="jen-arch-h">
          <div class="jah-stage">
            <span class="jah-tag">① 输入</span>
            <h5>schema.sql</h5>
            <p>建表语句 / 数据库连接</p>
            <div class="jah-code">CREATE TABLE `t_user` (<br>&nbsp;&nbsp;`id` bigint UNSIGNED ...,<br>&nbsp;&nbsp;`name` varchar(128) ...,<br>&nbsp;&nbsp;UNIQUE KEY `uk_name` ...<br>);</div>
          </div>
          <div class="jah-arr">→</div>
          <div class="jah-stage">
            <span class="jah-tag">② 解析</span>
            <h5>statement_parser</h5>
            <p>自研 SQL parser，输出结构化 Schema</p>
            <div class="jah-code">Schema {<br>&nbsp;&nbsp;Table · Column[]<br>&nbsp;&nbsp;Index · UniqueKey<br>&nbsp;&nbsp;TypeMap (MySQL→Go)<br>}</div>
          </div>
          <div class="jah-arr">→</div>
          <div class="jah-stage">
            <span class="jah-tag">③ 渲染</span>
            <h5>generator × template</h5>
            <p>5 个 generator × 2 套框架模板</p>
            <div class="jah-grid">
              <span>generate_po</span>
              <span>generate_dto</span>
              <span>generate_vo</span>
              <span>generate_dao</span>
              <span>generator_tool</span>
            </div>
            <p class="jah-hint">framework: itea-go | gorm</p>
          </div>
          <div class="jah-arr">→</div>
          <div class="jah-stage jah-wide">
            <span class="jah-tag">④ 产物</span>
            <h5>原子方法集</h5>
            <div class="jah-out">
              <span>Insert</span><span>Update</span><span>Delete</span>
              <span>GetByPK</span><span>GetByUK</span><span>GetByIdx</span>
              <span>List</span><span>LikeQuery</span><span>CustomCond</span>
              <span>WithTx</span><span>BatchInsert</span><span>Count</span>
            </div>
            <p class="jah-hint">主键 / 唯一索引 / 普通索引 分别派生独立查询方法</p>
          </div>
        </div>
        <p class="dim" style="font-size:14px;margin-top:6px;text-align:center">源码：<code>github.com/LingoJack/model_infrax</code></p>
      </div>
    </div>
    <aside class="notes">
      <p>jen 的原理就四步：输入 schema、自研 parser 解析索引语义、用 Go template 渲染、输出按主键/唯一索引/普通索引派生的原子方法集。确定性输出、零字段漂移。</p>
    </aside>
  </section>

  <!-- ============ §5.4 jen 实测对比 ============ -->
  <section class="slide" data-title="jen Benchmark">
    <div class="title-bar">
      <h2>05 · 模板代码生成引擎 — jen 实测对比
    </div>
    <div class="split s55">
       <div class="col-text" style="overflow:hidden">
        <h4 style="margin-top:0;margin-bottom:2px">验证方式</h4>
        <p style="margin-bottom:2px">51 张表 DAO 全生成。同一份 schema.sql，对照：<strong>①</strong> Agent 逐文件写；<strong>②</strong> Agent 加载 jen Skill，调 jen + 验收。</p>
        <h4 style="margin-bottom:2px">速度更快，代码质量更高</h4>
        <p style="margin-bottom:2px">两者都编译通过，<strong>差异在过程成本和字段精度</strong>。LLM 直写需反复"字段对齐→方法补全→编译修复"——<strong>长上下文维护本身成为负担</strong>。67 次 compact 说明上下文已频繁高水位。</p>
        <h4 style="margin-bottom:2px">字段漂移 · 系统性风险</h4>
        <p style="margin-bottom:2px">11 个字段遗漏、4 个索引方法缺失——写到第 30 张表时 Agent 已忘了第 5 张表的唯一索引。jen 以 schema 为唯一事实源，从根本消除漂移。</p>
        <h4 style="margin-bottom:2px">结论</h4>
        <p>确定性生成器<strong>把规则稳定、易漂移的部分从 LLM 链路剥离</strong>，让 Agent 回归需求理解和集成验收。</p>
      </div>
      <div class="col-visual">
        <div style="margin-top:0">
          <table class="bench-table">
            <thead>
              <tr>
                <th>维度</th>
                <th class="col-bad">LLM 直接生成</th>
                <th class="col-good">jen + Skill</th>
                <th class="col-delta">改善</th>
              </tr>
            </thead>
            <tbody>
              <tr><td>编译通过</td><td>通过</td><td>通过</td><td>—</td></tr>
              <tr><td>字段一致性</td><td>247 / 258 = 95.7%</td><td><strong>258 / 258 = 100%</strong></td><td class="delta">+4.3 pp</td></tr>
              <tr><td>CRUD 覆盖率</td><td>100%</td><td>100%</td><td>—</td></tr>
              <tr><td>索引查询覆盖</td><td>22 / 26 = 84.6%</td><td><strong>26 / 26 = 100%</strong></td><td class="delta">+15.4 pp</td></tr>
              <tr class="hl"><td>LLM 调用次数</td><td>582</td><td><strong>11</strong></td><td class="delta">↓ 53×</td></tr>
              <tr class="hl"><td>工具调用次数</td><td>2,006</td><td><strong>15</strong></td><td class="delta">↓ 134×</td></tr>
              <tr class="hl"><td>Token 消耗</td><td>24,219,892</td><td><strong>483,775</strong></td><td class="delta">↓ 50×</td></tr>
              <tr class="hl"><td>会话总耗时</td><td>53 min 5 sec</td><td><strong>1 min 56 sec</strong></td><td class="delta">↓ 27×</td></tr>
              <tr><td>压缩触发次数</td><td>67</td><td>0</td><td class="delta">—</td></tr>
              <tr><td>人工修正次数</td><td>0（2 次网络重试不计）</td><td>0</td><td>—</td></tr>
            </tbody>
          </table>
          <p class="dim" style="font-size:11px;margin-top:6px">论文表 7-8 / 7-9 · 51 张表 DAO 全生成 · 数据来源：metrics.json + transcript.jsonl</p>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>同一份 schema 做对照实测，jen 把 token 降到了 1/50、耗时降到 1/27、字段一致性 100%。这里我想强调的是——引入它不是为了替代 Agent，而是把规则稳定、容易漂移的部分从 LLM 链路里剥离出来，让模型可以专注在业务逻辑和契约定义上。</p>
    </aside>
  </section>

  <!-- ============ §5.5 webapp-gen 完整工作流 ============ -->
  <section class="slide" data-title="webapp-gen Flow">
    <div class="title-bar">
      <h2>05 · 模板代码生成引擎 — webapp-gen 全栈工作流
    </div>
    <div class="split s55">
      <div class="col-text" style="justify-content:center">
        <h4 style="margin-top:0">设计思路</h4>
        <p>不是"AI 一次性写完一个 App"，而是一条<strong>11 步固化工作流</strong>。把大模型不擅长的部分交给模板，专注需求理解、契约定义、业务逻辑、增量集成。</p>
        <h4>三个关键设计</h4>
        <ul style="line-height:1.8;padding-left:16px">
          <li><strong>脚手架前置</strong> · 模板保证结构完整性，避免 AI 从零搭建框架</li>
          <li><strong>原型反馈闭环</strong> · Step 4–5 先用 mock 出可视原型，用户迭代确认后再写后端</li>
          <li><strong>三环境矩阵验收</strong> · 本地 + 单容器 + 全栈 compose，同一份测试脚本三环境都要过</li>
        </ul>
        <h4>jen Skill 嵌入</h4>
        <p>Step 6 写完 SQL schema 后调 <code>@skill:jen</code> 生成 DAO 层，确定性部分从 LLM 链路剥离。</p>
      </div>
      <div class="col-visual" style="display:flex;align-items:center;overflow:visible">
        <div class="webapp-pipe" style="margin-top:0">
          <div class="wp-row">
            <div class="wp-step wp-init">
              <span class="wp-num">0</span>
              <h5>项目初始化</h5>
              <p><code>git clone</code> 模板<br>React+Go 骨架</p>
            </div>
            <div class="wp-arr">→</div>
            <div class="wp-step">
              <span class="wp-num">1</span>
              <h5>需求分析</h5>
              <p>一句话 → requirement.md<br>用例 + 非功能需求</p>
            </div>
            <div class="wp-arr">→</div>
            <div class="wp-step">
              <span class="wp-num">2</span>
              <h5>API 设计</h5>
              <p>REST 契约<br>统一信封 / 错误码</p>
            </div>
            <div class="wp-arr">→</div>
            <div class="wp-step">
              <span class="wp-num">3</span>
              <h5>前端设计</h5>
              <p>路由 / 组件<br>状态管理方案</p>
            </div>
          </div>
          <div class="wp-row">
            <div class="wp-step wp-loop">
              <span class="wp-num">4</span>
              <h5>原型生成</h5>
              <p>mock 数据填充<br><code>npx tsc --noEmit</code></p>
            </div>
            <div class="wp-arr">⇄</div>
            <div class="wp-step wp-loop">
              <span class="wp-num">5</span>
              <h5>原型反馈</h5>
              <p>Ask 用户审视<br>迭代直到确认</p>
            </div>
            <div class="wp-arr">→</div>
            <div class="wp-step">
              <span class="wp-num">6</span>
              <h5>后端设计</h5>
              <p>SQL schema<br><span class="hl-skill">@skill:jen</span> → DAO</p>
            </div>
            <div class="wp-arr">→</div>
            <div class="wp-step">
              <span class="wp-num">7</span>
              <h5>后端编码</h5>
              <p>controller / service<br>Podman MySQL</p>
            </div>
          </div>
          <div class="wp-row">
            <div class="wp-step wp-test">
              <span class="wp-num">8</span>
              <h5>黑盒测试</h5>
              <p><code>api-test.sh</code><br>CRUD + 鉴权 + 边界</p>
            </div>
            <div class="wp-arr">→</div>
            <div class="wp-step">
              <span class="wp-num">9</span>
              <h5>前端编码</h5>
              <p>mock → 真实 API<br>snake ↔ camelCase</p>
            </div>
            <div class="wp-arr">→</div>
            <div class="wp-step wp-final">
              <span class="wp-num">10</span>
              <h5>容器化验收</h5>
              <p><code>podman compose up</code><br>三环境矩阵</p>
            </div>
            <div class="wp-arr">→</div>
            <div class="wp-step wp-result">
              <span class="wp-num">✓</span>
              <h5>可交付制品</h5>
              <p>gradiation_<br>artifact_demo</p>
            </div>
          </div>
        </div>
      </div>
    </div>
    <aside class="notes">
      <p>把 jen 嵌进一个更大的流程，就是 webapp-gen——我固化的一条 11 步流水线：模板脚手架前置 → 文档驱动定契约 → mock 原型反馈闭环 → jen 生成数据层 → 后端编码 → 三环境矩阵容器化验收。背后的思路就一句话：AI 不擅长的交给模板，AI 只做需求理解、契约定义和集成验收。</p>
    </aside>
  </section>

  <!-- ============ §06 分隔页 ============ -->
  <section class="slide chapter-divider" data-title="§06 Divider">
    <span class="ch-num">Section 06</span>
    <div class="ch-title">总结与展望</div>
    <p class="ch-sub">四大贡献、开源仓库、未来方向。</p>
  </section>

  <!-- ============ 13.9. REPO GALLERY ============ -->
  <section class="slide" data-title="Repo Gallery">
    <div class="title-bar">
      <h2>06 · 总结与展望 — 项目仓库
    </h2>
    </div>
    <div class="repo-list">
      <div class="rl-item">
        <span class="rl-url">github.com/LingoJack/jcli</span>
        <span class="rl-name">jcli · 主项目</span>
        <span class="rl-desc">Rust · 5.2 万行 · 上下文治理 / 工具强化 / 多 Agent / Skill & Hook</span>
      </div>
      <div class="rl-item">
        <span class="rl-url">github.com/LingoJack/model_infrax</span>
        <span class="rl-name">jen · 模板代码引擎</span>
        <span class="rl-desc">Go · 自研 SQL parser + Go template · 封装为 Skill</span>
      </div>
      <div class="rl-item">
        <span class="rl-url">github.com/LingoJack/gradiation_artifact_demo</span>
        <span class="rl-name">artifact_demo · 端到端验证</span>
        <span class="rl-desc">React + Go + GORM + Podman · webapp-gen 全流程生成 · 三环境验收通过</span>
      </div>
    </div>
    <p class="rl-note">所有代码开源 · MIT License</p>
    <div class="deck-footer">
      <span class="mono">github.com/LingoJack</span>
      <span class="slide-number" data-current="25" data-total="37"></span>
    </div>
    <aside class="notes">
      <p>毕设产出最终是三个开源仓库：主项目 jcli，Rust 写的，大约 5.2 万行；模板引擎 model_infrax，也就是 jen；以及端到端验证制品 gradiation_artifact_demo。</p>
    </aside>
  </section>

  <!-- ============ 13.10. DEMO PREVIEW ============ -->
  <section class="slide" data-title="Demo Preview">
    <div class="title-bar">
      <h2>06 · 总结与展望 — 交付制品预览
    </h2>
    </div>
    <div class="demo-preview">
      <img src="pics/preview-1.png" alt="制品预览 1">
      <img src="pics/preview-2.png" alt="制品预览 2">
      <img src="pics/preview-3.png" alt="制品预览 3">
      <img src="pics/preview-4.png" alt="制品预览 4">
      <img src="pics/preview-5.png" alt="制品预览 5">
      <img src="pics/preview-6.png" alt="制品预览 6">
    </div>
    <p class="dp-caption">gradiation_artifact_demo · React + Go + GORM + Podman · 三环境验收通过</p>
    <div class="deck-footer">
      <span class="mono">github.com/LingoJack</span>
      <span class="slide-number" data-current="26" data-total="37"></span>
    </div>
    <aside class="notes">
      <p>这是用 jcli 配合 webapp-gen 和 jen 真实生成出来的一个电商 Web 应用。技术栈是 React + Go + GORM + Podman，三环境验收都跑通了。</p>
    </aside>
  </section>

  <!-- ============ 14. SUMMARY ============ -->
  <section class="slide" data-title="Summary">
    <div class="title-bar">
      <h2>06 · 总结与展望 — 四项主要工作
    </div>
    <div style="display:flex;flex:1;min-height:0;overflow:hidden;gap:0">
      <!-- 四行内容 -->
      <div style="flex:1;display:flex;flex-direction:column;gap:0;justify-content:center">
        <div style="border:2.5px solid var(--accent);border-radius:4px;padding:16px 20px;position:relative">
          <div style="margin-bottom:16px">
            <h4 style="margin:0">上下文治理与状态感知</h4>
            <p class="dim" style="margin:4px 0 0;font-size:15px;line-height:1.6">分级消息窗口 + 多级压缩 + 豁免高价值消息 + 动态状态感知注入。让 Agent 在长任务中<strong>持续低幻觉运行</strong>。</p>
          </div>
          <div>
            <h4 style="margin:0">面向场景强化的工具</h4>
            <p class="dim" style="margin:4px 0 0;font-size:15px;line-height:1.6">Grep / Ask / Plan / Edit / 执行三态 / system-reminder / Hook。共同保障单 Agent 的<strong>长时间工程闭环</strong>。</p>
          </div>
          <span style="position:absolute;right:-4px;top:50%;transform:translate(100%,-50%);font-size:12px;font-weight:600;color:var(--accent);white-space:nowrap;padding-left:6px">单 Agent</span>
        </div>
        <div style="padding:18px 20px">
          <h4 style="margin:0">多 Agent 协作机制</h4>
          <p class="dim" style="margin:4px 0 0;font-size:15px;line-height:1.6">有了单 Agent 能力，多 Agent 协作需要<strong>消息同步与上下文隔离</strong>。Teammate + SubAgent + SendMessage Gate，三组实验全部通过。</p>
        </div>
        <div style="padding:0 20px">
          <h4 style="margin:0">模板代码生成引擎</h4>
          <p class="dim" style="margin:4px 0 0;font-size:15px;line-height:1.6">即使有了多 Agent，业务软件中仍有<strong>规则确定但不适合 LLM 推理的部分</strong>。jen 以 schema 为事实源确定性生成，token ↓50×、耗时 ↓27×、字段一致性 100%。</p>
        </div>
      </div>
    </div>
    <!-- 底部递进线：只用箭头 -->
    <p style="margin-top:14px;font-size:14px;text-align:center;color:var(--text-2)">
      <span style="color:var(--accent);font-weight:500">单 Agent 增强</span>
      <span style="margin:0 12px;color:var(--accent)">→</span>
      <span style="color:var(--text-2)">多 Agent 协作</span>
      <span style="margin:0 12px;color:var(--accent)">→</span>
      <span style="color:var(--text-2)">确定性生成补位</span>
    </p>
    <aside class="notes">
      <p>最后简单收一下。我做的四项工作之间是有递进关系的：上下文治理 + 工具强化保障了单 Agent 的长时运行；在此之上，多 Agent 协作提供了消息同步和上下文隔离；再在此之上，模板代码生成用确定性输出补上了 LLM 的短板。</p>
    </aside>
  </section>

  <!-- ============ 15. THANKS ============ -->
  <section class="slide cover" data-title="Thanks">
    <div class="center" style="flex-direction:column;height:100%">
      <p class="kicker">thanks</p>
      <h1 class="h1 anim-fade-up" data-anim="fade-up" style="text-align:center">谢谢聆听<br><span class="accent">Q &amp; A</span></h1>
      <div class="row wrap mt-l" style="justify-content:center">
        <span class="pill pill-accent">github.com/LingoJack/jcli</span>
        <span class="pill">github.com/LingoJack/model_infrax</span>
        <span class="pill">github.com/LingoJack/gradiation_artifact_demo</span>
      </div>
      <p class="lede mt-l" style="text-align:center;max-width:60ch">Agent：通过上下文治理和工具调用，<br>沟通物理世界与逻辑世界。</p>
    </div>
    <div class="deck-footer">
      <span class="mono">温俊杰 · 2025</span>
      <span class="slide-number" data-current="27" data-total="36"></span>
    </div>
    <aside class="notes">
      <p>我的答辩分享到此结束，欢迎各位老师批评指正。谢谢！</p>
    </aside>
  </section>

</div>

<script src="../../.jcli/skills/html-ppt-skill/assets/runtime.js"></script>
</body>
</html>
�世界和逻辑世界之间的桥梁。三个仓库都已经开源，欢迎各位老师批评指正。谢谢！</p>
    </aside>
  </section>

</div>

<script src="../../.jcli/skills/html-ppt-skill/assets/runtime.js"></script>
</body>
</html>
